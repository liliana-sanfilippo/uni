%! Author = lili
%! Date = 4/30/26


\section{Definition and Basic Properties}
de bruijn subgraph
DBG(s,k)  = (V,E)

V 0 alle k-mere von s

Wie viele k-mere hat eine Sequenz der Länge n sollte man wissen: n-k+1


\section{Words with the same (k + 1)-mer Profile}
info: für jeden DB Graph gibt es immer einen Eulerpfad

\begin{figure}{H}
    \begin{subfigure}{0.33\textwidth}
        \begin{tikzpicture}[
            node distance=2cm,
            every node/.style={circle},
            label distance=2pt,
            >=Stealth
        ]

            \node (gat) at (-3.31,-3.94) {GAT};
            \node (att) at (-2.37,-1.37) {ATT};
            \node (tta) at (0,0) {TTA};
            \node (tac) at (2.37,-1.37) {TAC};
            \node (aca) at (3.31,-3.94) {ACA};

            \draw[->] (gat) to[bend left=20] (att);
            \draw[->] (att) to[bend left=20] (tta);
            \draw[->] (tta) -- (att);

            \draw[->] (tta) to[bend left=10] (tac);
            \draw[->] (tac) to[bend left=10] (aca);

        \end{tikzpicture}
        \caption{$x = GATTACA, k = 3$}
    \end{subfigure}
    \begin{subfigure}{0.33\textwidth}
        \begin{tikzpicture}[
            node distance=2cm,
            every node/.style={circle},
            label distance=2pt,
            >=Stealth
        ]

            \node (gat) at (-3.31,-3.94) {GAT};
            \node (att) at (-2.37,-1.37) {ATT};
            \node (tta) at (0,0) {TTA};
            \node (tac) at (2.37,-1.37) {TAC};
            \node (aca) at (3.31,-3.94) {ACA};
            \node (tat) at (-0.5,-1.5) {TAT};

            \draw[->] (gat) to[bend left=20] (att);
            \draw[->] (att) to[bend left=50] (tta);
            \draw[->] (att) -- (tta);

            \draw[->] (tta) to[bend left=10]  (tat);
            \draw[->] (tat) to[bend left=10]   (att);

            \draw[->] (tta) to[bend left=10] (tac);
            \draw[->] (tac) to[bend left=10] (aca);

        \end{tikzpicture}
        \caption{$x = GATTATTACA,  k = 3$}
    \end{subfigure}
    \begin{subfigure}{0.33\textwidth}
        \begin{tikzpicture}[
            node distance=2cm,
            every node/.style={circle},
            label distance=2pt,
            >=Stealth
        ]
            \node (gat) at (-3.31,-3.94) {GAT};
            \node (att) at (-2.37,-1.37) {ATT};
            \node (tta) at (0,0) {TTA};
            \node (tac) at (2.37,-1.37) {TAC};
            \node (aca) at (3.31,-3.94) {ACA};
            \node (tat) at (-0.5,-1.5) {TAT};

            \node (taa) at (-0.8,1.3) {TAA};
            \node (aat) at (-2.5,0.6) {AAT};

            \draw[->] (gat) to[bend left=20] (att);
            \draw[->] (att) to[bend left=50] (tta);

            \draw[->] (aat) to[bend right=50] (att);
            \draw[->] (tta) to[bend right=50] (taa);
            \draw[->] (taa) to[bend right=20] (aat);

            \draw[->] (att) -- (tta);
            \draw[->] (att) to[bend left=20] (tta);

            \draw[->] (tta) to[bend left=10]  (tat);
            \draw[->] (tat) to[bend left=10]   (att);

            \draw[->] (tta) to[bend left=10] (tac);
            \draw[->] (tac) to[bend left=10] (aca);

        \end{tikzpicture}
        \caption{$x = GATTATTAATTACA, k = 3$}
    \end{subfigure}
\end{figure}

\begin{theo}
    Theorem 2.1 auf deutsch: \# worte mit gleichen (k+1)-mer Profil wie s = \# Euler-Pfade in DBG(s,k)
\end{theo}

\begin{kom}
    man könnte bei bsp 2.1. denken hey bei a kann es nur einen EP geben, weil es kein Kreis ist, und bei 2b
    mus es dann
    mehrere geben, eil da eine Schleife drin ist, abre NEIN der Pfad ist die Reihenfolge der Knoten und nicht der
    Kanten, also gibt es nur einen (visualisieren) Man kann nicht unterscheiden, ob man zuerst die obere oder untere
    Kante der Schleife zuerst gegangen ist. Gibt also nur eine sequenz mit em k+1 meer profil. Denn die Seuenz wird
    ja aus den Knoten gemacht, nicht den Kanten...sozusagen.
\end{kom}

\begin{kom}
    Der Begriff dbg ist sehr dehnbar, es ist in der Literatur nicht immer das gleiche damit gemeint.
\end{kom}


\section{Variants of de Bruijn Graphs}

\defin{origdb}

\begin{exa}
    zu origdb
\end{exa}

\defin{dbsg}

\begin{exa}
    zu dbsg
\end{exa}

\begin{prob}
    mit dem bi dings, anhang von bsp auf notiz
\end{prob}

\defin{bidbsg}

\begin{exa}
    zu bidbsg
\end{exa}


\defin{comdbsg}

\begin{exa}
    zu comdbsg
\end{exa}

\defin{coldbsg}

\begin{exa}
    zu coldbsg
\end{exa}

\begin{prob}
    log der alphabetgröße zur basis 2 ist wie viele bits man braucht zum codieren
    k*2 bits für dbg, aber mit Farben muss man ja noch bits dran hängen, das ist groß und noch ungelöst, denn es ist
    dann k*2 + \# datensätze
\end{prob}

\begin{exa}
    bsp zu bits: acg codiert als 000110 wenn

    \begin{tabular}{lll}
        A & 0 & 0 \\
        C & 0 & 1 \\
        G & 1 & 0 \\
        T & 1 & 1
    \end{tabular}

\end{exa}


\section{Implementation of de Bruijn Graphs: Sets of k-mers}


