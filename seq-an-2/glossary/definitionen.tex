\newglossaryentry{vorlage}{
    name={vorlage},
    description={},
    type=defi
}

\newglossaryentry{dimension}{
    name={dimension of a \gls{dbsg}},
    description={},
    type=defi
}

\newglossaryentry{origdb}{
name={de Bruijn graph},
    first={original de Bruijn graph of dimension k over a finite alphabet $\Sigma$ of size $\sigma$},
description={The graph that contains one vertex for each k-mer (and therefore $\sigma^k$ vertices) and a
directed edge for each overlap of length k − 1 (and therefore $\sigma^{k+1}$ edges)},
type=defi
}


\newglossaryentry{dbsg}{
name={de Bruijn subgraph},
description={},
type=defi,
symbol={DBG}
}


\newglossaryentry{bidbsg}{
name={bidirected de Bruijn subgraph},
description={},
type=defi
}


\newglossaryentry{comdbsg}{
name={compacted de Bruijn subgraph},
description={},
type=defi
}

\newglossaryentry{coldbsg}{
name={colored de Bruijn subgraph},
description={},
type=defi
}


\newglossaryentry{ranking function}{
name={ranking function},
description={A bijective function $r: \mathcal{X} \rightarrow [0, |\mathcal{X}|-1]$ for a finite set $\mathcal{X}$},
type=defi,
symbol={\ensuremath{r}}
}

\newglossaryentry{unranking function}{
name={unranking function},
description={The inverse of a \gls{ranking function}},
type=defi
}

\newglossaryentry{ranking algorithm}{
name={ranking algorithm},
description={An algorithm that implements a \gls{ranking function}},
type=defi
}

\newglossaryentry{unranking algorithm}
name={unranking algorithm},
description={An algorithm that implements a \gls{unranking function}},
type=defi
}

\newglossaryentry{dense}{
name={},
description={},
type=defi
}


\newglossaryentry{q-gram}{
name={$q$-gram},
description={},
type=defi
}


\newglossaryentry{occurrence count}{
name={occurrence count},
description={Let $x \in \sum^m$ and choose $q \in [1, m]$. The occurrence count of a q-gram $z \in \sum^q$ in x is
the number
\[
Occ(x, z) := |{i \in [1, m - q + 1] : x[i . . . i + q - 1] = z}|
\]
},
type=defi
}


\newglossaryentry{q-gram profile}{
name={q-gram profile},
description={Let $\sigma = |\sum|$. The q-gram profile of x is a $\sigma^q$-element vector $p_q(x) := (p_q(x)_z)_{z\in\sum^q}$, indexed by all q-grams, defined by $p_q(x)_z := Occ(x, z)$},
type=defi
}

\newglossaryentry{q-gram distance}{
name={q-gram distance},
description={
The q-gram distance of $x \in \sum^m$ and $y \in \sum^n$ is defined for $1 \leq q \leq min\{m, n\}$ as
\[d_q(x, y) := \sum_{z\in\sum^q}
|p_q(x)_z - p_q(y)_z |
\]},
type=defi,
symbol={\ensuremath{d_q}}
}



\newglossaryentry{alignment distance}{
name={alignment distance},
description={The alignment distance of two sequences $x,y \in \sum^*$ is defined as $d(x, y) := min\{cost(A) :
A \in \mathcal{A}*$  is an alignment of  $x$  and $y\}$},
type=defi
}



\newglossaryentry{edit distance}{
name={edit distance},
description={test},
type=defi
}