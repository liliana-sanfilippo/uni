# Aufgabe 1 (q-Gram-Distanz)

## Weshalb ist es sinnvoll, einem q-Gram einen Rang (engl. rank ) zuzuordnen?
Das erlaubt bestimmte q-Gramme zu mit erhöhter Prio zu betrachten und in der Betrachtung bzw. Analyse stärker zu gewichten.


## Ein Rang eines q-Grams an Position i kann aus dem Rang des vorhergehenden q-Grams an Position i−1 und dem “neuen” Zeichen an Position i+q−1 berechnet werden. Warum ist diese Vorgehensweise sinnvoll? Implementiere die vorgegebene Funktion get_rank (update_rank). Entscheide dich dabei konsistent entweder für aufsteigendes oder absteigendes ranking.

Sinnvoll, weil man dann insgesamt weniger Rechnen muss, ist sparsam die schon berechneten Sachen wiederzuverwenden.

 * Speichereffizienz
 * Eineindeutigkeit durch den Rank
 * Sucheffizienz
 * Schnellere Berechnung durch einfachere Vergleiche

## Wie lang ist ein q-Gram-Profil in Abhängigkeit von der Alphabetgröße und q? Vervollständige die Funktion get_profile, welche das q-Gram-Profil eines Strings s zurückgibt.

## Implementiere die vorgegebene Funktion d_q_gram, welche die Distanz zwischen zwei Strings a und b auf Basis ihrer q-Gram-Profile berechnen soll.

# Aufgabe 2 (Maximal Matches Distanz)

## Implementiere die Funktion left_to_right_partition(x, y).
Gegeben sind zwei Strings x und y. Bestimme mithilfe des Suffixbaums y$ die Left-to-Right-Partition von x bezüglich y. 
Zerlege dazu x von links nach rechts iterativ in möglichst lange Teilstrings, die jeweils als Substring in y
vorkommen. Nutze dazu die Funktion search_prefix der Suffixbaumimplementierung.
Gib die Partition als alternierende Liste von Strings und Einzelzeichen zurück.

## Implementiere die Funktion partition_size(partition).
Gegeben ist eine Partition von x in Form
einer alternierenden Liste aus Substrings (Matches in y) und einzelnen Zeichen (Nicht-Matches).
Berechne die Partitionsgröße gemäß der Vorlesungsdefinition (z. B. als Anzahl der Blöcke in der
Partition) und gib diese als Integer zurück.

## Implementiere die Funktion d_maximal_matches(x, y), die die maximal matches Distanz berechnen soll, mithilfe der in den vorangegangenen Teilaufgaben implementierten Funktionen.

# Aufgabe 3 (Laufzeitverhalten)

## Miss nun zusätzlich die Laufzeiten von d_maximal_matches und d_q_gram auf den zufällig generierten Stringpaaren RANDOM_PAIRS.
Speichere jeweils Stringlänge und gemessene Laufzeit und ergänze
die entsprechenden Kurven im Plot.

## Führe die gleichen Laufzeitmessungen auch für die adversarial erzeugten Stringpaare ADVERSARIAL_PAIRS durch.
Miss die Laufzeiten für d_edit, d_maximal_matches und d_q_gram separat und
erzeuge einen entsprechenden Plot.

## Für welche der Distanzfunktionen gibt es einen Unterschied zwischen zufälligen und adversarialen Strings? Warum?/Warum nicht?