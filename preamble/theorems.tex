
\usepackage{amsthm}

\tcbset{blue box/.style={
    enhanced, fonttitle=\bfseries,
    colback=white, colframe=accdarklight,
    coltitle=white, colbacktitle=accdark,
    attach boxed title to top left={xshift=0.3cm,
    yshift*=-\tcboxedtitleheight/2},
    boxed title style={
        before upper=\hspace*{0.5cm}, % reserve space for the image
        overlay={
            \node at ([xshift=0.5cm]frame.west) {};

        }
    }
}
}
\newtcolorbox{mybox}[1][]{blue box, #1}

\newtheorem{eig}{Eigenschaften}[chapter]


\newtheoremstyle{mytheoremstyle} % name
    {\topsep}                    % Space above
    {\topsep}                    % Space below
    {}                   % Body font
    {}                           % Indent amount
    {\bfseries}                   % Theorem head font
    {}                          % Punctuation after theorem head
    {1.5em}                       % Space after theorem head
    {\thmname{#1}\thmnumber{ #2}\thmnote{ #3}}  % Theorem head spec (can be left empty, meaning ‘normal’)
\theoremstyle{mytheoremstyle}


\newtheorem{test}{Test}[chapter]

%% BEISPIEL

\newtheorem{bsp}{Beispiel}[chapter]

\tcolorboxenvironment{bsp}{
    colback=bspf!05,
    colframe=bspf,
    boxrule=1pt,
    arc=2mm,
    top=1mm,
    bottom=1mm,
    left=1mm,
    right=1mm,
    enhanced,
    breakable,
    parbox=false,
    before skip=6pt,
    after skip=6pt
}


\newtheorem{info}{Zusatzinformation}[chapter]

%% REGELN

\newtheorem{reg}{Regel}[chapter]
\tcolorboxenvironment{reg}{
    colback=Thistle!05,
    colframe=Thistle,
    boxrule=1pt,
    arc=2mm,
    top=1mm,
    bottom=1mm,
    left=1mm,
    right=1mm,
    enhanced,
    breakable,
    parbox=false,
    before skip=6pt,
    after skip=6pt
}

\newtheorem{auf}{Aufgabe}[chapter]
\tcolorboxenvironment{auf}{
    colback=auff!05,
    colframe=auff,
    boxrule=1pt,
    arc=2mm,
    top=1mm,
    bottom=1mm,
    left=1mm,
    right=1mm,
    enhanced,
    breakable,
    parbox=false,
    before skip=6pt,
    after skip=6pt
}

\newtheorem{kor}{Korollar}[chapter]

%% LEMMA


\newtheorem{lem}{Lemma}[chapter]
\tcolorboxenvironment{lem}{
    colback=lemf!05,
    colframe=lemf,
    boxrule=1pt,
    arc=2mm,
    top=1mm,
    bottom=1mm,
    left=1mm,
    right=1mm,
    enhanced,
    breakable,
    parbox=false,
    before skip=6pt,
    after skip=6pt
}

\newtheorem{kom}{Kommentar}[chapter]


%% SATZ

\newtheorem{satz}{Satz}[chapter]
\tcolorboxenvironment{satz}{
    colback=satzf!05,
    colframe=satzf,
    boxrule=1pt,
    arc=2mm,
    top=1mm,
    bottom=1mm,
    left=1mm,
    right=1mm,
    enhanced,
    breakable,
    parbox=false,
    before skip=6pt,
    after skip=6pt
}


\newcommand{\satzbeweis}[1]{%
    \begin{satz}\label{satz:#1}
    \textbf{\gls*{#1}}\\
    \glsdesc*{#1}
    \paragraph{Beweis.} \glssymbol*{#1}
    \end{satz}
}

\newtheorem{theo}{Theorem}[chapter]
\tcolorboxenvironment{theo}{
    colback=bemf!05,
    colframe=bemf,
    boxrule=1pt,
    arc=2mm,
    top=1mm,
    bottom=1mm,
    left=1mm,
    right=1mm,
    enhanced,
    breakable,
    parbox=false,
    before skip=6pt,
    after skip=6pt
}

%\newtheorem{formel}[defi]{Definition}
%\newtheorem{gese}{Gesetz}[section]

%% DEFINITION

\newtheorem{defi}{Definition}[chapter]
\tcolorboxenvironment{defi}{
    colback=defif!05,
    colframe=defif,
    boxrule=1pt,
    arc=2mm,
    top=1mm,
    bottom=1mm,
    left=1mm,
    right=1mm,
    enhanced,
    breakable,
    parbox=false,
    before skip=6pt,
    after skip=6pt
}

\newtheorem{notation}{Notation}[chapter]
\tcolorboxenvironment{notation}{
    colback=defif!05,
    colframe=defif,
    boxrule=1pt,
    arc=2mm,
    top=1mm,
    bottom=1mm,
    left=1mm,
    right=1mm,
    enhanced,
    breakable,
    parbox=false,
    before skip=6pt,
    after skip=6pt
}

\newtheorem{konv}{Konvention}[chapter]
\tcolorboxenvironment{konv}{
    colback=defif!05,
    colframe=defif,
    boxrule=1pt,
    arc=2mm,
    top=1mm,
    bottom=1mm,
    left=1mm,
    right=1mm,
    enhanced,
    breakable,
    parbox=false,
    before skip=6pt,
    after skip=6pt
}

\newcommand{\defin}[1]{%
    \begin{defi}\label{def:#1}
    \textbf{\gls*{#1}}\\
    \glsdesc*{#1}
    \end{defi}
}
\newcommand{\deffin}[1]{%
    \begin{defi}\label{def:#1}
    \textbf{\glsentryfirst{#1}}\\
    \glsdesc*{#1}
    \end{defi}
}

%% EXAMPLE

\newtheorem{exa}{Example}[chapter]
\tcolorboxenvironment{exa}{
    colback=bspf!05,
    colframe=bspf,
    boxrule=1pt,
    arc=2mm,
    top=1mm,
    bottom=1mm,
    left=1mm,
    right=1mm,
    enhanced,
    breakable,
    parbox=false,
    before skip=6pt,
    after skip=6pt
}



%% PROBLEM

\newtheorem{prob}{Problem}[chapter]
\tcolorboxenvironment{prob}{
    colback=Rhodamine!05,
    colframe=Rhodamine,
    boxrule=1pt,
    arc=2mm,
    top=1mm,
    bottom=1mm,
    left=1mm,
    right=1mm,
    enhanced,
    breakable,
    parbox=false,
    before skip=6pt,
    after skip=6pt
}
\newcommand{\probl}[2]{
\begin{prob} \textbf{#1} \\
   {#2}
\end{prob}
}


%% FRAGE

\newtheorem{fr}{Frage}[chapter]
\tcolorboxenvironment{fr}{
    colback=Periwinkle!05,
    colframe=Periwinkle,
    boxrule=1pt,
    arc=2mm,
    top=1mm,
    bottom=1mm,
    left=1mm,
    right=1mm,
    enhanced,
    breakable,
    parbox=false,
    before skip=6pt,
    after skip=6pt
}
\newcommand{\frage}[1]{
    \begin{fr}
    \textbf{\gls*{#1}}  \\
    \glsdesc*{#1}
    \end{fr}
}


%% BEOBACHTUNG

\newtheorem{beo}{Beobachtung}[chapter]
\tcolorboxenvironment{beo}{
    colback=bemf!05,
    colframe=bemf,
    boxrule=1pt,
    arc=2mm,
    top=1mm,
    bottom=1mm,
    left=1mm,
    right=1mm,
    enhanced,
    breakable,
    parbox=false,
    before skip=6pt,
    after skip=6pt
}

\newtheorem{obs}{Observation}[chapter]
\tcolorboxenvironment{obs}{
    colback=bemf!05,
    colframe=bemf,
    boxrule=1pt,
    arc=2mm,
    top=1mm,
    bottom=1mm,
    left=1mm,
    right=1mm,
    enhanced,
    breakable,
    parbox=false,
    before skip=6pt,
    after skip=6pt
}

\newtheorem{prinz}{Prinzip}[chapter]
\tcolorboxenvironment{prinz}{
    colback=satzf!05,
    colframe=satzf,
    boxrule=1pt,
    arc=2mm,
    top=1mm,
    bottom=1mm,
    left=1mm,
    right=1mm,
    enhanced,
    breakable,
    parbox=false,
    before skip=6pt,
    after skip=6pt
}


\newtheorem{bem}{Bemerkung}[chapter]
\tcolorboxenvironment{bem}{
    colback=bemf!05,
    colframe=bemf,
    boxrule=1pt,
    arc=2mm,
    top=1mm,
    bottom=1mm,
    left=1mm,
    right=1mm,
    enhanced,
    breakable,
    parbox=false,
    before skip=6pt,
    after skip=6pt
}

\newtheorem{notte}{Note}[chapter]
\tcolorboxenvironment{notte}{
    colback=bemf!05,
    colframe=bemf,
    boxrule=1pt,
    arc=2mm,
    top=1mm,
    bottom=1mm,
    left=1mm,
    right=1mm,
    enhanced,
    breakable,
    parbox=false,
    before skip=6pt,
    after skip=6pt
}

\newtheorem{folg}{Folgerung}[chapter]
\tcolorboxenvironment{folg}{
    colback=bemf!05,
    colframe=bemf,
    boxrule=1pt,
    arc=2mm,
    top=1mm,
    bottom=1mm,
    left=1mm,
    right=1mm,
    enhanced,
    breakable,
    parbox=false,
    before skip=6pt,
    after skip=6pt
}

\newtheorem{imp}{Implication}[chapter]
\tcolorboxenvironment{imp}{
    colback=bemf!05,
    colframe=bemf,
    boxrule=1pt,
    arc=2mm,
    top=1mm,
    bottom=1mm,
    left=1mm,
    right=1mm,
    enhanced,
    breakable,
    parbox=false,
    before skip=6pt,
    after skip=6pt
}


\newtheorem{erg}{Ergänzung}[chapter]
\tcolorboxenvironment{erg}{
    colback=bemf!10,
    colframe=bemf,
    boxrule=1pt,
    arc=2mm,
    top=1mm,
    bottom=1mm,
    left=1mm,
    right=1mm,
    enhanced,
    breakable,
    parbox=false,
    before skip=6pt,
    after skip=6pt
}


\newtheorem{er}{Erinnerung}[chapter]
\tcolorboxenvironment{er}{
    colback=Orchid!05,
    colframe=Orchid,
    boxrule=1pt,
    arc=2mm,
    top=1mm,
    bottom=1mm,
    left=1mm,
    right=1mm,
    enhanced,
    breakable,
    parbox=false,
    before skip=6pt,
    after skip=6pt
}



\newcommand{\regel}[1]{
    \begin{reg} \textbf{\gls*{#1}}  \newline
    \glsdesc*{#1}
    \end{reg}
}
\newcommand{\prin}[2]{
    \begin{prinz} \textbf{#1}  \newline
    #2
    \end{prinz}
}
\newcommand{\rsp}[3]{
    \noindent \textbf{#1} \pageref{rsp:#2}

    \noindent #3

}