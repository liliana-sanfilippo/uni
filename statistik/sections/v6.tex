%! Author = lili
%! Date = 4/27/26

% TODO alte Folien noch einbauen

\untit{Vorlesung 6 in 2025 / Vorlesung 22.05.2026}
\begin{bsp}
    \textbf{Die Randverteilungen legen die gemeinsame Verteilung nicht eindeutig fest} \\
    Wir werfen eine faire Münze zweimal.
    Die Ergebnisse der beiden Würfe sollen notiert werden. \\
    Alex schreibt korrekt auf, während Kim einen Moment lang abgelenkt ist, dadurch das Ergebnis des
    zweiten Wurfs verpasst und als Notlösung einfach so tut, als hätten beide Würfe dasselbe Ergebnis
    gehabt. \\
    \textit{Modellierung}
    \[
        \eraum = \{0,1\}^2
    \]
    mit Gleichverteilung $\pe$. \\
    \textit{Zufallsvariable}
    \begin{itemize}
        \item Alex notiert den zweifachen Münzwurf \\ $X : \eraum \rightarrow \eraum$ mit $X = (X_1, X_2)$ wobei
        $X_1((\ele_1, \ele_2)) = \ele_1$ und $X_2((\ele_1, \ele_2)) = \ele_2$ für alle $\oio$ \\ (das heißt,
        $X$ ist die Identität auf $\eraum$)
        \item Kim notiert nur den ersten Münzwurf \\ $Y : \eraum \rightarrow \eraum$ mit $Y = (Y_1, Y_2)$ wobei
        $Y_1((\ele_1, \ele_2)) = \ele_1$ und $Y_2((\ele_1, \ele_2)) = Y_1((\ele_1, \ele_2)) = \ele_1$ für alle $\oio$
    \end{itemize}
    \begin{table}[H]
        \begin{tabular}{cccc}
            $\wfk_X ((x_1, x_2))$ & $x_2 = 0$     & $x_2 = 1$     & $\wfk_{X_1}(x_1)$ \\
            $x_1 = 0$             & $\frac{1}{4}$ & $\frac{1}{4}$ & $\frac{1}{2}$     \\
            $x_1 = 1$             & $\frac{1}{4}$ & $\frac{1}{4}$ & $\frac{1}{2}$     \\
            $\wfk_{X_2}(x_2)$     & $\frac{1}{2}$ & $\frac{1}{2}$ & 1
        \end{tabular}

        \begin{tabular}{cccc}
            $\wfk_Y ((y_1, y_2))$ & $y_2 = 0$     & $y_2 = 1$     & $\wfk_{Y_1}(y_1)$ \\
            $y_1 = 0$             & $\frac{1}{2}$ & 0             & $\frac{1}{2}$     \\
            $y_1 = 1$             & 0             & $\frac{1}{2}$ & $\frac{1}{2}$     \\
            $\wfk_{Y_2}(y_2)$     & $\frac{1}{2}$ & $\frac{1}{2}$ & 1
        \end{tabular}\label{tab:table}
    \end{table}
    Offensichtlich sind die Randverteilungen von $X$ und von $Y$ gleich, während die gemeinsame Verteilung
    von $Y_1$ und $Y_2$ ist nicht dieselbe ist wie jene von $X_1$ und $X_2$.
\end{bsp}

\begin{bsp}
    \textbf{n-mal würfeln mit fairem Würfel} \\
    $\eraum \{1,\dots,6\}^n, \quad \ele_i$ das Ereignis des i-ten Wurfs. $\pe$ gleichverteilt. \\
    $X_i: \eraum \rightarrow \{1,\dots ,6 \}, \quad X_i(\ele) = \ele_i \quad \forall i \in \{1,\dots ,6 \}$ \\
    z.z. $X_i, \dots, X_n$ unabhängig \\
    \begin{align*}
        \pe(X_1=k_1, ..., X_n= k_n) & \quad \text{ für } k_i \in \{1,..,6\} \\
        \pe(\{(k_1,..,K_n)\}) & = \frac{1}{6^n}\\
        \pe(X_i = k_i)  & =
        \pe(X_1\in \{1,..,6\}, ..., X_{i-1} \in \{1,..,6\}, X_i = k_i, X_{i+1} \in \{1,..,6\}, ..., X_n \in \{1,..,6\}) \\
        & = \pe(\{1,..,6\}^{i-1} \times ...) \\
        & = \frac{6^{n-1}}{6^n}
        & = \frac{1}{6}
    \end{align*}
    \[
        \pe(X_1 = k_1, ..., X_n = k_n)  = \pe(X_1 = k_1) \cdot \dots \cdot \pe(X_n = k_n)
    \]
    \txo{! jetzt müssen wir noch prüfen: Paare, Tripel, ...}
    Danach: $X_1, \dots, X_n$ bewiesenermaßen unabhängig \\
    Beispielsweise:
    $\Rightarrow \{x_1 \in \{2,4,6\}, \dots, \{x_4 \leq 4\}\}$ unabhängig \\
    % über arrow "z.B."
\end{bsp}
Frage: müssen wir die Paare und Tripel wirklich prüfen?


\begin{satz}
    Seien $X_1, \dots, X_n$ unabhängig $\Rightarrow \pe(X_1 \in B_1 , \dots , X_n \in B_n) =  \pe(X_1 \in B_1) \cdot \dots \cdot \pe(X_n \in B_n)$

    \paragraph{Beweis.} Um zu zeigen, dass \[\pe(X_1 \in B_1, X_3 = B_3) = \pe(X_1 = B_3) \cdot \pe(X_3 = B_3)\]
    gilt, wählen wir
    \[B_2 = E_2, B_4 = E_4 , \dots, B_n = E_n\]

\end{satz}
\begin{defi}
    \textbf{Unabhängigkeit von Zufallsvariablen} \\
    Auf einem Wahrscheinlichkeitsraum $\wraum$ seien Zufallsvariablen $X_1, .., X_n$ gegeben mit $X_i : \eraum E_i$ für
    $i = 1, .., n$. \\
    Die Zufallsvariablen $X_1, .., X_n$ heißen unabhängig, wenn die Ereignisse
    \[
        \{X_1 \in B\}, \dots , \{X_n \in B\}
    \]
    für alle Wahlen von messbaren Mengen $B_i \subseteq E_i, i = 1, \dots, n$, unabhängig sind.
\end{defi}
Diese Definition ist zunächst einmal abschreckend.
Wir haben bereits gesehen, wie viele Bedingungen für den Nachweis der Unabhängigkeit von n Ereignissen zu prüfen sind.
Und nun sollen wir diesen Aufwand auch noch für jede Wahl von $B_i$ betreiben!
Wir werden uns daher im folgenden darum bemühen, einfacher nachzuweisende Kriterien für Unabhängigkeit herzuleiten.

Als erstes halten wir fest, dass es im diskreten Fall genügt, einelementige Mengen $B_i$ zu betrachten.

\subsection{Unabhängigkeit für Zufallsvariable mit abzählbarem Bildbereich}\label{subsec:unabhangigkeit-fur-zufallsvariable-mit-abzahlbarem-bildbereich}
\komm{In 2025 und 2026 wurden diese Informationen verschieden formuliert. Hier befinden sich alle drei Versionen und eine zusammengeführte Version. Es handelt sich dabei um verschiedene Charackterisierungen der Unabhängigkeit. Es sind danach auch noch weitere Charackterisierungen bzw. Konsequenzen zu finden.}
\begin{satz}
    \label{satz:satz-i}
    \textbf{Satz I. (2025) - Charakterisierung über Ereignisse} \\
    Seien $X_1, \dots, X_n$ \gls{zv} auf $(\glssymbol{eraum} , \pe); X_i$ sei $E_i$-wertig. \\
    Dann
    \[
        X_1, \dots, X_n \text{ unabhängig } \Leftrightarrow \kl{X_1 = x_1}, \dots, \kl{X_n = x_n}  \text{ unabhängig } \forall x_i \in E_i (i=1, \dots, n)
    \]

    \paragraph{Beweis.} \texttt{Siehe Folie 2 (2025)}
\end{satz}


\begin{satz}
    \label{satz:satz-ii}
    \textbf{Satz II. (2025)} (Neuformulierung von Satz I. - Charakterisierung über Produktform) \\
    Haben $X_1, \dots, X_n$ abzählbare Werte-/Bildbereiche.
    Dann
    \[
        X_1, \dots, X_n  \text{ unabhängig } \Leftrightarrow \glssymbol{wfk}_{X_1, \dots, X_n} ((x_1, \dots, x_n)) = \prod^n_{i=1} \glssymbol{wfk}_{x_i}(x_i)
    \]
\end{satz}


\begin{theo}
    \label{theo:unabhängigkeit-zv-abzählbar}
    \textbf{Unabhängigkeit für Zufallsvariable mit abzählbarem Bildbereich (2026)} \\
    Auf einem \gls{wraum} $\wraum$ seinen \glspl{zv} $X_1, .., X_n$ mit abzählbarem Bild-
    bereich gegeben.
    Dann sind  $X_1, .., X_n$ genau dann unabhängig, wenn die Ereignisse
    \[
        \{X_1 = x_1\}, \dots , \{X_n = x_n\}
    \]
    für alle $x_i \in X_i(\eraum), i = 1, .., n$ unabhängig sind.

    \paragraph{Beweis.} Siehe Skript S. 58.
\end{theo}

\komm{
    Gemeinsam haben alle drei Formulierungen:
    \begin{center}
        $X_1, .., X_n$ sind \glspl{zv} auf \gls{wraum} $\wraum$
    \end{center}
    Verschieden formuliert sind:
    \begin{center}
        ``haben abzählbare Werte-/Bildbereiche'' $\Leftrightarrow$ $X_i$ sei $E_i$-wertig
    \end{center}
    und
    \[
        X_1, \dots, X_n \text{ unabhängig } \Leftrightarrow \overbrace{\underbrace{\kl{X_1 = x_1}, \dots, \kl{X_n = x_n}  \text{ unabhängig } \forall x_i \in E_i (i=1, \dots, n)}_{
            \glssymbol{wfk}_{X_1, \dots, X_n} ((x_1, \dots, x_n)) = \prod^n_{i=1} \glssymbol{wfk}_{x_i}(x_i)
        }}^{\text{wenn die Ereignisse }
        \{X_1 = x_1\}, \dots , \{X_n = x_n\}
        \text{ für alle } x_i \in X_i(\eraum), i = 1, .., n \text{  unabhängig sind.}}
    \]
}


\begin{kor}
    \textbf{Konsequenz aus Satz \ref{satz:satz-ii}} \\
    Abzählbarer Bildbereich \\
    Es genügt
    \[\pe(X_1 = x_1 , ..., X_n = x_n) = \pe (X_1 = x_1) \cdot \dots \cdot \pe(X_n = x_n) \forall x_i \in X_i(\eraum)\]
    zu zeigen, um Unabhängigkeit
    von $X_1, \dots X_n $ zu zeigen.
\end{kor}

% todo satz von tafel, insgesamt alles von der Tafel

\begin{satz}
    \label{satz:charaktersierung-verteilungsfunktion}
    \textbf{Charakterisierung über die Produktform der Verteilungsfunktion} \\
    $X_1, .., X_n$ unabhängig
    \begin{align*}
        \Leftrightarrow \pe ( X_1 \leq a_1 , ..., X_n \leq a_n ) & = \pe(X_1 \leq a_1) \cdot \dots \cdot
        \pe(X_n \leq a_n) \\
        & =  F_{X_1}(a_1) \cdot \dots \cdot F_{X_n}(a_n \quad \forall a_i \in \rz)
    \end{align*}
    % Fehler? vermischung von F und \pe
\end{satz}

\begin{kor}
    \label{kor:unabh-gem-verteilung-eindeutig}
    \textbf{Konsequenz aus Satz \ref{satz:charaktersierung-verteilungsfunktion}} \\
    $X_1, .., X_n$ unabh. $\Leftrightarrow$ gemeinsame Verteilung eindeutig bestimmt durch (Rand-)Verteilungen von
    $X_1, .., X_n$ \\
    Aka.:
    \[
        X_1, \dots, X_n  \text{ unabh. } \Rightarrow \text{ gemeinsame Verteilung von } X_1, \dots, X_n \text{ eindeutig bestimmt durch die Randverteilung}
    \]
\end{kor}

\begin{bem}
    Zu Korollar \autoref{kor:unabh-gem-verteilung-eindeutig} \\
    Das Korollar sagt uns, wie wir unabhängige Zufallsvariablen mit Zähldichte modellieren. \\
    Ohne Unabhängigkeit ist das nicht der Fall!
\end{bem}

\begin{bem}
    \textbf{Erinnerung an mathematisches Kriterium für Unabhängigkeit} \\
    Wie haben gesehen, dass Unabhängigkeit bereits gezeigt ist, wenn
    $\pe(X_1 = x_1, \dots, X_n = x_n) = \pe(X_1 = x_1) \cdot .. \cdot \pe(X_n = x_n) \forall x_i \in E_i (i = 1, \dots, n)$ gilt.
    \txc{Wenn also die Produktform existiert.}
    (Es ist nicht erforderlich $X_{i_1}, \dots, X_{i_k}$ separat zu betrachten.)
\end{bem}

\begin{bem}
    \textbf{Anwendung des mathematischen Kriteriums für Unabhängigkeit}
    \textbf{(a)} Wenn wir die Produktform finden: Beweis der Unabh.
    \txc{Wenn man also zeigen kann $\pe_{X_1, \dots, X_n} = \prod_i \pe_{X_i}$ } \\
    \txc{Umkehridee:} \\
    \textbf{(b)} Gegeb.
    Verteilung der $X_i$: Wir nutzen Produktform, um die gemeinsame Verteilung für unabh. $X_i$
    UZ BESTIMMWEN Todo inddex Produktform
    \txc{Also wenn die $X_i$ unabhängig sind und die Randverteilungen bekannt sind, dann kann man die gemeinsame Verteilung berechnen durch
        \[
            \pe_{X_1, \dots, X_n} (x_1, .., x_n) = \prod_i \pe_{X_i}(x_i)
        \]
    }
\end{bem}
% Tafel:
$X_1, .., X_n$ mit Dichten, Unabh.
dann
\[
    \pe(X_1 \in a_1, X_n \in a_n) = siehebild (aber das zweite)
\]

\subsection{Poisson-Verteilte ZV}\label{subsec:poisson-verteilte-zv}

\begin{satz} (2025)
    \\
    Summe unabh.\ Poisson-verteilter \gls{zv} ist wieder Poisson-verteilt. \\
    Seien $X_1, X_2$ unabh.\ und Poisson-verteilt mit
    \begin{align*}
        \glssymbol{wfk}_{x_1}(k) = \pe(X_1 = k) & = \frac{z_1^k}{k!} e^{-z_1} & (z_1 > 0) \forall k \in \mathbb{N}_0 \\
        \glssymbol{wfk}_{x_2}(k) = \pe(X_2 = k) & = \frac{z_2^k}{k!} e^{-z_2} & (z_2 > 0) \forall k \in \mathbb{N}_0
    \end{align*}
    Dann gilt: $X_1 + X_2$ ist Poisson-Verteilt mit Parameter $z_1 + z_2$.

    \paragraph{Beweis.} \texttt{Siehe Folie 6--7}
\end{satz}


\begin{bsp}
    Verteilung der Summe von zwei unabh., poisson.verteilt, \zv \\
    $X_1, X_2 unabh.$
    \[
        \mu_{x_!} (k) = \pe(X_! = k) = \frac{\lambda^k_1}{k!} e^{-\lambda_1} \quad \forall k \in \nz_0
    \]
    das gleiche fpr i = 2 % TODO
\end{bsp}

\begin{bem}
    $X_1 + X_2$ ist poisson verteilz mit Parameter $\lambda_1 + \lambda_2 $
    \[
        \mu_{X_1 + X_2}(n) = \pe(X_1 + X_2 = n) \quad \text{ für } n \in \nz_0 \\
        = \pe \big( \bigcup^k_{k_1 = 0} \{ X_1 = k_1 , X_2 = n - k_1\} \big)
        = \sum1 n_{k_1=0} \pe(X_1 = k_1, X_2 = n - k_1)
        = Rest bild \dots % TODO
    \]
    da muss noch ein + in den cup % TODO
    \komm{$X_1 + X_2$ nehmen beide wete aus n 0 an, also ist die Summe auch da drin }
\end{bem}


\section{\gls{zv} mit Dichten}\label{sec:zv-mit-dichten}

\begin{satz}
    \textbf{Satz III. (2025): Unabhängigkeiten von \gls{zv}} (mit und ohne Dichte) \\
    Seien $X_1, \dots, X_n$ unabh. $ \Leftrightarrow \pe(X_1 \leq a_1, \dots, X_n \leq a_n) = \pe(X_1 \leq a_1) \cdot \dots \cdot  \pe(X_n \leq a_n) = \mathcal{F}_{x_1}(a_1) \cdot \dots \cdot \mathcal{F}_{x_n}(a_n) \forall a_1, \dots, a_n \in \mathbb{R}$.

    \paragraph{Beweis.}
    \texttt{Siehe Folie 8}
\end{satz}


\begin{satz}
    \label{satz:satz-iv}
    \textbf{Satz IV. (2025 + 2026)} \\
    $X_1, \dots, X_n$ mit Dichten \index{Dichte} $f_1, \dots, f_n$.
    Dann gilt:
    $X_1, \dots, X_n$ unabh. $\Leftrightarrow$ gemeinsame \dichte hat Produktform $\Leftrightarrow$ $(x_1, \dots, x_n) \mapsto f((x_1, \dots, x_n)) \coloneqq  f_1(x_1)\cdot \dots \cdot f_n(x_n)$ eine gemeinsame Sichte von $X_1, \dots, X_n$ ist.
\end{satz}

\begin{bem}
    Zu Satz \autoref{satz:satz-iv} \\
    \begin{itemize}
        \item Existenz der gem. \dichte ist nicht vorausgesetzt.
        Gegeben $X_1, \dots, X_n$ mit Dichten \index{Dichte} $f_1, \dots, f_n$
        \item Setze
        \begin{equation}
            \label{eq:gemdichte}
            f(x_1, \dots, x_n) \coloneqq  f_1(x_1) \cdot \dots \cdot f_n(x_n) = \prod^n_{i=1} f_i(x_1) \quad \forall
            x_i \in
            \mathbb{R} (i=1, \dots, n)
        \end{equation}
        Aussage des Satzes ist: $X_1, \dots, X_n$ unabh. $\Leftrightarrow f$ gem. \dichte von $X_1, \dots, X_n$ ist
        \item $X_1, \dots, X_n$ mit Dichten \index{Dichte} $f_1, \dots, f_n$.
        Modelliere unabhängige Experimente durch gemeinsame \dichte \eqref{eq:gemdichte}.
        \item Gegen eine gem. \dichte $f$ für $X_1, \dots, X_n$ berechne Randdichten $f_{x_1}, \dots, f_{x_n}$. \\
        Gilt Gleichung~\eqref{eq:gemdichte}? Falls ja: $X_1, \dots, X_n$ unabhängig.
        Falls~\eqref{eq:gemdichte} für $(x_1, \dots, x_n)$ aus einer Menge $; \subseteq \mathbb{R}^n$ gilt mit $\mathbb{R}^n\backslash M$ abzählbar, dann gilt Unabh.
    \end{itemize}
\end{bem}

\komm{Vorsicht: Eine Wahrscheinlichkeitsdichte ist nur ``fast überall'' eindeutig. \\ Also: Die Produktform muss nicht an jedem einzelnen Punkt gelten.
Es genügt, wenn sie fast überall gilt.
}

%\begin{satz} \textbf{für ZV mit Dichte (2026)}\\
%$X_1, .., X_n$ ZV mit Dichten $f_1, .., f_n$.
%Dann gilt
%    \[
%        X_1, .., X_n \Rightarrow \txo{"gemDichte hat produktform"} \Rightarrow  BOLD
%    \]
%\end{satz}


\section{Widerlegen von Unabhängigkeiten}\label{sec:widerlegen-von-unabhangigkeiten}
\begin{bem}
    \label{bem:widerlegen}
    Es genügt nicht einen Punkt zu finden, in dem (\ref{eq:gemdichte}) nicht gilt.
    Wir brauchen eine Menge $D \subseteq \mathbb{R}^n$ mit $|D| = Z(D) > 0$, so dass (\ref{eq:gemdichte}) auf $D$ nicht gilt. \\
    Einfacher: Zeigen, dass $\pe(X_1 \leq a_1, \dots, X_n \leq a_n) \neq \prod^n_{i=1} F_{x_i} (a_i)$. \\
    Zeigen Sie, dass die Produktform \textbf{nicht} gilt auf Quader oder verwenden Sie die (nicht geltende) Produktform der Verteilungsfunktion.
\end{bem}
\komm{
    Zu Bemerkung \autoref{bem:widerlegen}: \\
    $\pe(X_1 \leq a_1, \dots, X_n \leq a_n) \neq \prod^n_{i=1} F_{x_i} (a_i)$ bedeutet, die gemeinsame Verteilungsfunktion faktorisiert nicht. Das ist gemeint mit ``verwenden Sie die (nicht geltende) Produktform der Verteilungsfunktion'', denn da
    $F_{X_1, \dots, X_n}(a_1, \dots, a_n) = \pe(X_1 \leq a_1, \dots, X_n \leq a_n)$ gilt, ist das die Negation der Produktform der Verteilungsfunktion.
}
\komm{
    Zu Bemerkung \autoref{bem:widerlegen}: \\
    Die Produktform der Verteilungsfunktion wird auf sogenannten
    ``Quadern'' geprüft. Damit sind Mengen der Form
    \[
        (-\infty,a_1]\times\cdots\times(-\infty,a_n]
        \subseteq \mathbb R^n
    \]
    gemeint. \\
    Für den Zufallsvektor $(X_1,\dots,X_n)$ entspricht ein solcher Quader dem Ereignis
    \[
        \{X_1\le a_1,\dots,X_n\le a_n\}.
    \]
    Die gemeinsame Verteilungsfunktion ist daher gerade
    \[
        F_{X_1,\dots,X_n}(a_1,\dots,a_n)
        =
        \mathbb \pe(X_1\le a_1,\dots,X_n\le a_n).
    \]
    Sind $X_1,\dots,X_n$ unabhängig, so zerfällt diese Wahrscheinlichkeit in ein Produkt:
    \[
        \mathbb \pe(X_1\le a_1,\dots,X_n\le a_n)
        =
        \prod_{i=1}^n \mathbb \pe(X_i\le a_i)
        =
        \prod_{i=1}^n F_{X_i}(a_i).
    \]
    Man sagt dann: \\
    Die Verteilungsfunktion besitzt Produktform auf Quadern. \\
    Um zu zeigen, dass Zufallsvariablen nicht unabhängig sind, genügt es daher,
    einen Quader zu finden, für den diese Produktform nicht gilt. \\
    \chatti
}
% todo 2025  Randdichten: BSP  \texttt{Hier fehlt was}


\komm{Bei Bild Tafel link ganz unten reicht es an einem Punkt zu zeigen, weil wir da ganze Mengen benutzt haben. Okay ????}

\begin{bsp}
    \textbf{uniform. verteilung auf Rechteck} \\
    Sei $(X,Y)$ ein zufälliger Punkt in $\rz$ (uniforme Verteilung)
    \begin{figure}[H]
        \includegraphics[width=0.6\linewidth]{./images/viereck}\label{fig:figure2}
    \end{figure}
    $(X,Y)$ hat (gem.) Dichte $f(x,y) = \chi_R (x,y) \frac{1}{a \cdot b} \quad \forall (x,y) \in \rz^2$
    Randdichten:
    \begin{align*}
        f_X(x) = \chi_{[0,a]}(x) \in_0^b \underbrace{f(x,y)}_{\frac{1}{a \cdot b}} dy & = \frac{1}{a} \chi_{[0,a]}(x)\\
        f_Y(y) & = \frac{1}{b} \chi_{[0,b]}(y)\\
        f(x,y)&  = \chi_{[0,a] \times [0,b]}(x,y)  \frac{1}{a \cot b} \\
        & = f_X(x) f_Y(y)
    \end{align*}
    schlauer:
    \[
        f(x,y) = \chi_{\rz} (x,y) \frac{1}{a \cot b} = \frac{1}{a} \underbrace{\chi_{[0,a]}(x) }_{\text{Randdichte von } X} \cdot \frac{1}{b} \underbrace{\chi_{[0,b]}(y) }_{\text{Randdichte von } Y}
    \]
    \txo{Wir müssen die Randdichten gar nicht berechnen!}
    X,Y unabh.
    \komm{wenn man sieht die Dichte zerfällt in x und y Argument muss man nicht integrieren, sondern nur schauen, dass die Funktionen (?) das richtige Gewicht haben. OK. Gerald fragen...} % TODO
\end{bsp}

\begin{bsp}
    \label{bsp:dreieck}
    Dreieck
    \begin{figure}[H]
        \includegraphics[width=0.6\linewidth]{./images/dreieck}\label{fig:dreieck}
    \end{figure}
    Sei $(X,Y)$ ein zufälliger Punkt \txc{gleichverteilt} im Dreieck $D$. \\
    \txc{Gleichverteilt bedeutet: Dichte ist konstant auf $D$ und 0 außerhalb.}
    \begin{align*}
        f(x,y) & = \frac{1}{|D|} \chi_D (x,y) \\
        & = 2 \chi_D(x,y) \\
        & = \begin{cases}
                2 & 0 \leq y \leq x \leq 1 \\ 0 & sonst
        \end{cases}
    \end{align*}
    Randdichten: \txc{(Man ``integriert die andere Variable weg'')})
    \begin{align*}
        f_X (x) & = \chi_{[0,1]}(x) \int_{- \infty}^{\infty} f(x,y) dy \\
        & = \chi_{[0,1]}(x) \int_{0}^{\txo{x}} 2 dy = 2 x  \chi_{[0,1]}(x) \\
        f_Y (y) & = \dots = 2(1-y)\chi_{[0,1]}(y)
    \end{align*}
    Wenn $X,Y$ unabhängig \txc{wären, dann würde gelten}:
    \txc{\begin{equation}
             f(x,y) = f_X(x)\,f_Y(y)\label{eq:wenn-unabhängig}
    \end{equation}}
    \txc{Einsetzen ergibt:}
    \begin{align*}
        f(x,y) & = 2 \cdot \chi_D(x,y) \\
        & \overset{?}{=} f_X(x) f_Y(y) \\
        & = 2x 2 (1-y) \chi_{[0,1]}(x,y)
    \end{align*}
    Wir sehen:
    Für $(x,y) \in [0,1]^2 \backslash D $ gilt $f(x,y) = 0$, also
    ``rechte Seite'' $> 0$. \\
    Keine Unabhängigkeit. \txc{Für Erklöärung siehe Kommentar \ref{kom:dreieck-erklärt}.} \\
    Alternative:
    \txc{Statt Dichten zu vergleichen, kann man Unabhängigkeit auch testen über:
        \[
            \mathbb \pe(X\le a, Y\le b) \stackrel{?}{=} F_X(a)F_Y(b)
        \]}
    Explizit: Berechne $F_X$ und $F_Y$, zeige, dass eis \textbf{einen} Punkt $(a,b) \in \rz^2$ gibt mit
    \[
        \pe(X \leq a, Y \leq b) \neq F_X(a) F_Y(b).
    \]
\end{bsp}

\komm{Symbole und Terme in Beispiel \autoref{bsp:dreieck}
    \begin{itemize}
        \item $\chi_D(x,y)$ = Indikatorfunktion:
        \[
            \chi_D(x,y)=
            \begin{cases}
                1 & (x,y)\in D \\
                0 & \text{sonst}
            \end{cases}
        \]
        \item $|D|$ = Fläche des Dreiecks (hier: $|D|=\tfrac12$)
        \item $f(x,y)$ = gemeinsame Dichte von $(X,Y)$
        \item $f_X(x)$ beschreibt die Verteilung von $X$ alleine
        \item $f_Y(y)$ beschreibt die Verteilung von $Y$ alleine
    \end{itemize}
}

\komm{ \label{kom:dreieck-erklärt}
Für $(x,y) \in [0,1]^2 \backslash D $ gilt $f(x,y) = 0$
    weil in der Dichtefunktion oben explizit festgelegt wird, dass es außerhalb des Dreiecks 0 ist und $[0,1]^2 \backslash D$ ist alles außer das Dreieck. \\ Also
    ist die ``rechte Seite''  $f_X(x)\,f_Y(y)$ aus \ref{eq:wenn-unabhängig}). \\
    Diese ist $> 0$, denn $x$ kann 0 sein (siehe Bild \ref{fig:dreieck}, 0 ist der innerste Randpunkt von ``außerhalb des Dreiecks'') und
    $y$ kann 1 sein (analog äußerster Randpunkt von ``außerhalb des Dreiecks''). Aber da es Randpunkte sind, haben sie keinen Einfluss auf die Dichte, denn Punkte haben keine Fläche und somit keine relevante ``Menge''. Also kann man diese Punkte
    wegignorieren und einfach sagen $x > 0$ und $y < 1$. Und wenn man das annimmt, dann erhält man:
    \[
        f_X(x) f_Y(y) = 4 x (1-y) \chi_{[0,1]^2}(x,y)
    \]
    Denn wir haben ja oben ausgerechnet, dass $f_X (x) = 2 x  \chi_{[0,1]}(x)$ und $f_Y (y) = 2(1-y)\chi_{[0,1]}(y)$, INSOFERN $X$ und $Y$ unabhängig sind. \\
    Weil aber $y<1$ gilt, muss $1-y>0$ sein, also
    \[
       \overbrace{ 4  \underbrace{x}_{> 0}  ( \underbrace{1-y}_{> 0} )}^{> 0}
    \]
Das ist dann ein Widerspruch damit, dass jeder Punkt außerhalb des Dreiecks $f(x,y) = 0$ hat. Wir haben einen Punkt außerhalb des Dreiecks betrachtet, und der hatte
$f_X(x) f_Y(y) > 0$. Also $ f(x,y) \neq f_X(x) f_Y(y)$, was äauivalet ist zu ``X und Y sind NICHT unabhängig''.
}

\komm{Zu Beispiel \autoref{bsp:dreieck} \\
\textbf{Warum reicht ein einzelner Punkt zur Widerlegung?}
Wenn Unabhängigkeit gilt, muss die Gleichung für \emph{alle} $(a,b)$ gelten. Findet man \emph{einen einzigen} Punkt $(a,b)$ mit Ungleichheit,
    ist Unabhängigkeit widerlegt. \\
    "Unabhängigkeit ist eine globale Eigenschaft und ein Gegenbeispiel genügt, um sie zu zerstören."
}

\paragraph{hier}

mehr bilder % TODO

Wir sehen:
Für $(x,y) \in [0,1]^2 \backslash D $ gilt $f(x,y) = 0$, aber ``rechte Seite'' $> 0$. \\
Keine Unabhängigkeit. \\
Alternative: berechne $F_X$ und $F_Y$, zeigem dass es einen (Un) Punkt $(a,b) \in \rz^2$ gibt mit
\[
    \pe(X \leq a, Y \leq b) \neq F_X(a) \cdot F_Y(b).
\]


\begin{defi}
    X,Y unabh. \zv.
    Dann heißt die Verteilung der Summe \textbf{Faltung} (der Verteilungen).
\end{defi}




