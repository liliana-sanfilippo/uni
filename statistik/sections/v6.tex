%! Author = lili
%! Date = 4/27/26

 % TODO alte Folien noch einbauen

\untit{Vorlesung 6 in 2025 / Vorlesung 22.05.2026}
\begin{bsp} \textbf{Die Randverteilungen legen die gemeinsame Verteilung nicht eindeutig fest} \\
    TODO
\end{bsp}

\begin{bsp} \textbf{n-mal würfeln} \\
    $\eraum \{1,\dots,6\}^n, \quad \ele_i$ das Eregenis des i-ten Wurfs. $\pe$ gleichverteilt. \\
    $X_i: \eraum \rightarrow \{1,\dots ,6 \}, \quad X_i(\ele) = \ele_i \quad \forall i \in \{1,\dots ,6 \}$ \\
    z.z. $X_i, \dots, X_n$ unab. \\
    \begin{align*}
        \pe(X_1=k_1, ..., X_n= k_n) \quad \text{ für } k_i \in 1-6 \\
        \pe(\{(k_1,..,K_n)\}) = \frac{1}{6^n}\\
        \pe(X_i = k_i) = \pe(X_!\in \{1,..,6\}, ..., X_{i-1} \in \{1,..,6\}, X_i = k_i, X_{i+1} \in \{1,..,6\}, ..., X_n \0n 1-6) = \e(\{1,..,6\}^{i-1} \times ...) = \frac{g^{n-1}}{6^n} = \frac{1}{6}\\
        \pe(X_1 = k_1, ...) = \pe(X_1 = k_1) \cdot \dots \cdot \pe(X)
        % TODO heir schauen ob das letzte richtig ist die hat das komisch angeschrieben\\
    \end{align*}
    \txo{! jetzt müssen wir noch prügen: Paare, tripel, ...}
    Danach: $X_!, \dots, X_n$ unabh. \\
    $\Rightarrow \{x_1 \in \{2,4,6\}, \dots, \{x_4 \leq 4\}\}$ unabh. \\
    % todo über arrow "z.B."
    Frage: müssen wir \ldots wirklich prüfen?
    % TODO BILD 1
    \paragraph{Beweis.} UM zu zeigen, dass \[\pe(X_1 \in B_1, X_3 = B_3) = \pe(X_1 = B_3) \cdot \pe(X_3 = B_3)\]
    gilt, wählen wir
    \[B_2 = E_2, B_4 = E_4 , \dots, B_n = E_n\]
\end{bsp}

\begin{defin} \textbf{nabhängigkeit von Zufallsvariablen} \\
    todo skript
\end{defin}

\begin{kor}
    Anz.
    Bildbereich \\
    Es genügt \[\pe(X_1 = x_1 , ..., X_n = x_n) = \pe (X_1 = x_1) \cdot \dots \cdot \pe(X_n = x_n) \forall x_i in X_i(\eraum)\]
    zz, um Unabh.
    von $X_1, \dots X_n $ zz
\end{kor}

% todo satz von tafel, insgesamt alles von der Tafel

\begin{satz}
    $X_1, .., X_n$ unabh. $\Leftrightarrow \pe ( X_1 \leq a_1 , ..., X_n \leq a_n ) = [das gleiche nur mit mals] = F_{X_1}(a_1) \cdot \dots \cdot \pe_{X_n}(a_n \quad \forall a_i \in \rz)$
    % TODO Fehler? vermischung von F und \pe
\end{satz}

\begin{kor}
    $X_1, .., X_n$ unabh. $\Leftrightarrow$ gemeinsame Verteilung eindeutig bestimmt durch (Rand-)Verteilungen von
    $X_1, .., X_n$
\end{kor}
\begin{bem}
    \paragraph{(a)} Wenn wir die Produktform finden: Beweis der Unabh.
    \paragraph{(b)} Gegeb.
    Verteilung der $X_i$: Wir nutzen Produktform, um die gemeinsame Verteilung für unabh. $X_i$
    UZ BESTIMMWEN  Todo inddex Produktform
\end{bem}
% Tafel:
$X_1, .., X_n$ mit DIchten, Unabh.
dann
\[
    \pe(X_1 \in a_1, X_n \in a_n) = siehebild (aber das zweite)
\]

% TODO die ganzen Sätze vergleichen ...
\begin{satz} für ZV mit Dichte \\
$X_1, .., X_n$ ZV mit Dichten $f_1, .., f_n$.
Dann gilt
    \[
        X_1, .., X_n \Rightarrow \txo{"gemDichte hat produktform"} \Rightarrow  BOLD % TODO
    \]
\end{satz}

\begin{bem}
    \paragraph{(a)} Existenz der gemeinsamen DIchte ist nicht vorausgesetzt: \\ Gegeben $f_1, .., f_n$ \\
    Satz $f(x_1, .., x_n) = komisches U sammelzeichen wir $ BILD % TODO
    \paragraph{(b)} BILD % todo
    \paragraph{(c)} gegeben gemeinsame Dichte: prüfe auf
    \txc{Vosichtig sein, denn Dichten sind ja nicht eindeutig.! (Bezug auf Ja, Nein)}
    \komm{Wenn das nicht gilt, dann kann das sein, weil es an einzelnen Punkten nicht gilt, deshalb reicht es nicht xxxx zu zeigen. Man sol in der Klaussur nicht hinschreiben "Offensichtlich gilt nicht Gleichheit" oder so für "diese" zahlen, das muss man schon zeigen.}
\end{bem}

Widerlegen bvon Unabh.
bei Dichten: zeigen Sie, Produktform \textbf{nicht} gilt auf Quader oder verwenden Sie die (
nicht geltende) Produktform der Verteilungsfunktion.
% TODO wtf ist die Produktform

\komm{Bei Bild Tafel link ganz unten reicht es an einem Punkt zu zeigen, weil wir da ganze Mengen benutzt haben. Okay ????}

\begin{bsp} \textbf{uniform. verteilung auf Rechteck}
    Bild Rechteck\ldots % TODO
    $(X,Y)$ hat (gem.) Dichte $f(x,y) = \Chi_R (x,y) \frac{1}{a \cdot b} \quad \forall (x,y) \in \rz^2$
    Randdichten:
    \begin{align*}
        f_X(x) = \Chi_{[0,a]}(x) \in_0^b \underbrace{f(x,y)}_{\frac{1}{a \cdot b}} dy = \frac{1}{a} \Chi_{[0,a]}(x)\\
        f_Y(y) = \frac{1}{b} \Chi_{[0,b]}(y)\\
    \end{align*}
BILD % TODO
    schlauer:
    \[
        f(x,y) =
    \]
\txo{Wir müssen die Randdichten gar nicht berechnen!}
X,Y unabh.
\komm{wenn man sieht die Dichte zerfällt in x und y Argument muss man nicht integrieren, sondern nur schauen, dass die Funktionen (?) das richtige Gewicht haben. OK. Gerald fragen...} % TODO
\end{bsp}

\begin{bsp} DREIECK \\
    BILD
\end{bsp}

Randdichten
\[
    f_X (x) = \Chi_{[0,1]}(x) \int .. BILD % TODO
\]

mehr bilder % TODO

Wir sehen:
Für $(x,y) \in [0,1]^2 \backslash D $ gilt $f(x,y) = 0$, aber ``rechte Seite'' $> 0$. \\
Keine Unabhängigkeit. \\
Alternative: berechne $F_X$ und $F_Y$, zeigem dass es einen (Un) Punkt $(a,b) \in \rz^2$ gibt mit
\[
    \pe(X \leq a, Y \leq b) \neq F_X(a) \cdot F_Y(b).
\]
\begin{bsp}
    Verteilung der Summe von zwei unabh., poisson.verteilt, \zv \\
    $X_1, X_2 unabh.$
    \[
        \mu_{x_!} (k) = \pe(X_! = k) = \frac{\lambda^k_1}{k!} e^{-\lambda_1} \quad \foall k \in \nz_0
    \]
    das gleiche fpr i = 2 % TODO
\end{bsp}

\begin{beh}
    $X_1 + X_2$ ist poisson verteilz mit Parameter $\lambda_1 + \lambda_2 $
    \[
        \mu_{X_1 + X_2}(n) = \pe(X_1 + X_2 = n) \quad \text{ für } n \in \nz_0 \\
        = \pe \big( \bigcup^k_{k_1 = 0} \{ X_1 = k_1 , X_2 = n - k_1\} \big)
        = \sum1 n_{k_1=0} \pe(X_1 = k_1, X_2 = n - k_1)
        = Rest bild \dots % TODO
    \]
    da muss noch ein + in den cup % TODO
    \komm{$X_1 + X_2$ nehmen beide wete aus n 0 an, also ist die Summe auch da drin }
\end{beh}

\begin{defi}
    X,Y unabh. \zv. Dann heipt die Verteilung der Summe \textbf{Faltung} (der Verteilungen).
\end{defi}

%%%%% Alt unten

\section{\gls{zv} mit abzählbarem Bildbereich}
\begin{satz}
    \textbf{Satz I.}
    Seien $X_1, \dots, X_n$ \gls{zv} auf $(\glssymbol{eraum} , \pe); X_i$ sei $E_i$-wertig. \\
    Dann
    \[
        X_1, \dots, X_n \text{ unabhängig } \Leftrightarrow \kl{X_1 = x_1}, \dots, \kl{X_n = x_n}  \text{ unabhängig } \forall x_i \in E_i (i=1, \dots, n)
    \]

    \paragraph{Beweis.} \texttt{Siehe Folie 2}
\end{satz}
\begin{bsp}
    \textbf{n-mal Würfeln (fairer Würfel)}
\end{bsp}
\begin{satz}
    \textbf{Satz II.} (Neuformulierung von Satz I.) \\
    Haben $X_1, \dots, X_n$ abzählbare Werte-/Bildbereiche.
    Dann
    \[
        X_1, \dots, X_n  \text{ unabhängig } \Leftrightarrow \glssymbol{wfk}_{X_1, \dots, X_n} ((X_1, \dots, X_n)) = \prod^n_{i=1} \glssymbol{wfk}_{x_i}(x_i)
    \]
\end{satz}
\begin{kor}
    \[
        X_1, \dots, X_n  \text{ unabh. } \Rightarrow \text{ gemeinsame Verteilung von } X_1, \dots, X_n \text{ eindeutig bestimmt durch die Randverteilung}
    \]
    \begin{bem}
        Das Korollar sagt uns, wie wir unabhängige Zufallsvariablen mit Zähldichte modellieren. \\
        Ohne Unabhängigkeit ist das nicht der Fall!
    \end{bem}
    \begin{bem}
        Wie haben gesehen, dass Unabhängigkeit bereits gezeigt ist, wenn $\pe(X_1 = x_1, \dots, X_n = x_n) = \pe(X_1 = x_1) \cdot .. \cdot \pe(X_n = x_n) \forall x_i \in E_i (i = 1, \dots, n)$ gilt. (Es ist nicht erforderlich  $X_{i_1}, \dots, X_{i_k}$ seperat zu betrachten.
    \end{bem}
\end{kor}
\begin{satz}
    Summe unabh.\ Poisson-verteilter \gls{zv} ist wieder Poisson-verteilt. \\
    Seien $X_1, X_2$ unabh.\ und Poisson-verteilt mit
    \begin{align*}
        \glssymbol{wfk}_{x_1}(k) = \pe(X_1 = k) & = \frac{z_1^k}{k!} e^{-z_1} & (z_1 > 0) \forall k \in \mathbb{N}_0 \\
        \glssymbol{wfk}_{x_2}(k) = \pe(X_2 = k) & = \frac{z_2^k}{k!} e^{-z_2} & (z_2 > 0) \forall k \in \mathbb{N}_0
    \end{align*}
    Dann gilt: $X_1 + X_2$ ist Poisson-Verteilt mit Parameter $z_1 + z_2$.

    \paragraph{Beweis.} \texttt{Siehe Folie 6--7}
\end{satz}

\section{\gls{zv} mit Dichten \index{Dichte}}
\begin{satz}
    \textbf{Satz III.: Unabhängigkeiten von \gls{zv}} (mit und ohne Dichte) \\
    Seien $X_1, \dots, X_n$ unabh. $ \Leftrightarrow \pe(X_1 \leq a_1, \dots, X_n \leq a_n) = \pe(X_1 \leq a_1) \cdot \dots \cdot  \pe(X_n \leq a_n) = \mathcal{F}_{x_1}(a_1) \cdot \dots \cdot \mathcal{F}_{x_n}(a_n) \forall a_1, \dots, a_n \in \mathbb{R}$.

    \paragraph{Beweis.}
    \texttt{Siehe Folie 8}
\end{satz}
\begin{satz}
    \textbf{Satz IV.} \\
    $X_1, \dots, X_n$ mit Dichten \index{Dichte} $f_1, \dots, f_n$. Dann gilt:
    $X_1, \dots, X_n$ unabh. $\Leftrightarrow$ gemeinsame \dichte hat Produktform $\Leftrightarrow$ $(x_1, \dots, x_n) \mapsto f((x_1, \dots, x_n)) \coloneqq  f_1(x_1)\cdot \dots \cdot f_n(x_n)$ eine gemeinsame Sichte von $X_1, \dots, X_n$ ist.
    \begin{bem}
        \begin{itemize}
            \item Existenz der gem. \dichte ist nicht vorausgesetzt. Gegeben $X_1, \dots, X_n$ mit Dichten \index{Dichte} $f_1, \dots, f_n$
            \item Setze
            \begin{equation}
                \label{eq:gemdichte}
                f(x_1, \dots, x_n) \coloneqq  f_1(x_1) \cdot \dots \cdot f_n(x_n) \forall x_i \in \mathbb{R} (i=1, \dots, n)
            \end{equation}
            Auusage des Satzes ist: $X_1, \dots, X_n$ unabh. $\Leftrightarrow f$ gem. \dichte von $X_1, \dots, X_n$ ist
            \item $X_1, \dots, X_n$ mit Dichten \index{Dichte} $f_1, \dots, f_n$. Modelliere unabh. Exp. durch gem. \dichte (\ref{eq:gemdichte}).
            \item Gegen eine gem. \dichte $f$ für $X_1, \dots, X_n$ berechne Randdichten $f_{x_1}, \dots, f_{x_n}$. \\ Gilt (\ref{eq:gemdichte})? Falls ja: $X_1, \dots, X_n$ unabh. Falls (\ref{eq:gemdichte}) für $(x_1, \dots, x_n)$ aus einer Menge $; \subseteq \mathbb{R}^n$ gilt mit $\mathbb{R}^n\backslash M$ abzählbar, dann gilt Unabh.
        \end{itemize}
    \end{bem}
\end{satz}

\section{Widerlegen von Unabhängigkeiten}
Es genpgt nicht einen Punkt zu finden, in dem (\ref{eq:gemdichte}) nicht gilt. Wir brauchen eine Menge $D \subseteq \mathbb{R}^n$ mit $|D| = Z(D) > 0$, so dass (\ref{eq:gemdichte}) auf $D$ nicht gilt. \\
Einfacher: Zeigen, dass $\pe(X_1 \leq a_1, \dots, X_n \leq a_n) \neq \prod^n_{i=1} F_{x_i} (a_i)$
Randdichten:
\begin{bsp}

\end{bsp}
Randdichten:
\texttt{Hier fehlt was}

