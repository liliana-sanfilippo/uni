%! Author = lili
%! Date = 7/9/26

\chapter{Tutorium 26.05.2026}
\untit{Präsenzaufgaben 1.5: Unabhängigkeit von Zufallsvariablen \hfill 26.--29.\ Mai 2026}
\section{PÜ 1.5.4.}
Seien $X$ und $Y$ zwei Zufallsvariablen mit einer gemeinsamen Dichte
$f : \rz^2 \to [0,\infty)$, gegeben durch
\[
    f(x,y) =
    \begin{cases}
        c\, e^{-\left(x + \frac{y}{2}\right)} & \text{falls } 0 \leq x \leq y \leq 1,\\[2pt]
        0 & \text{sonst,}
    \end{cases}
\]
mit $c \in \rz$.

\begin{auf}[Bestimmen Sie $c \in \rz$ so, dass $f$ eine Dichte ist.]
    \index{Aufgabe!Dichte!Bestimmen Sie ein c derart dass f eine Dichte ist}
    NOCH KEINE LÖSUNG % TODO NOCH KEINE LÖSUNG
\end{auf}

\begin{auf}[Bestimmen Sie die Randdichten von $X$ und $Y$.]
    \index{Aufgabe!Dichte!Randdichte bestimmen}
    NOCH KEINE LÖSUNG % TODO NOCH KEINE LÖSUNG
\end{auf}

\begin{auf}[Bestimmen Sie die Verteilungsfunktionen von $X$ und $Y$.]
    \index{Aufgabe!Verteilungsfunktion bestimmen}
    NOCH KEINE LÖSUNG % TODO NOCH KEINE LÖSUNG
\end{auf}

\begin{auf}[Sind die Zufallsvariablen $X$ und $Y$ unabhängig?]
    \index{Aufgabe!Unabhängigkeit bewerten}
    NOCH KEINE LÖSUNG % TODO NOCH KEINE LÖSUNG
\end{auf}

\begin{auf}[Berechnen Sie die Wahrscheinlichkeit $\pe(X > Y)$.]
    \index{Aufgabe!Wahrscheinlichkeit berechnen}
    NOCH KEINE LÖSUNG % TODO NOCH KEINE LÖSUNG
\end{auf}

\section{PÜ 1.5.5.}

\begin{auf}[Seien $X, Y, Z$ unabhängige Zufallsvariable.
Beweisen Sie, dass $\max\{X,Y\}$ und $Z$ unabhängig sind.]
    \index{Aufgabe!Unabhängigkeit beweisen}
    NOCH KEINE LÖSUNG % TODO NOCH KEINE LÖSUNG
\end{auf}

\begin{auf}[Beweisen oder widerlegen Sie:
Sind $X, Y, Z$ beliebige unabhängige Zufallsvariable, so sind auch die
Zufallsvariablen $\max\{X,Y\}$ und $\max\{X,Z\}$ unabhängig.]
    NOCH KEINE LÖSUNG % TODO NOCH KEINE LÖSUNG
\end{auf}