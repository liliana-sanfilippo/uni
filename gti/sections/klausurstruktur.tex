

\begin{mytable}
    \begin{tabular}{@{}c l l@{}}
        \hline
        \textbf{Aufg.} & \textbf{Thema} & \textbf{Kernkompetenz}\\
        \hline
        1 & Sprachunterscheidung & Chomsky-Hierarchie, Pumping-Lemma, Grammatiken, RegExp\\
        2 & Endliche Automaten & NEA/DEA, Potenzmengenkonstr., Abschluss-Konstruktionen\\
        3 & Kontextfreie Gramm.\ \& LR/Kellerautomaten & Eindeutigkeit, LR-Parsing, Strukturbaum, CYK, PDA\\
        4 & Probleme / Berechenbarkeit \& Reduktion & P, NP, $\le_p$, Reduktionen, Chomsky-Größen\\
        5 & Multiple Choice & Querschnitt über den ganzen Stoff\\
        \hline
    \end{tabular}
\end{mytable}

\section{Sprachunterscheidung}\label{subsec:sprachunterscheidung}
Zu einer Sprache $L$ entscheiden und beweisen, in welche Klasse sie fällt
(regulär / nicht regulär, kontextfrei / nicht kontextfrei).
Meist 3-4 Teilaufgaben, die die vier Beweisrichtungen kombinieren.
\begin{bem}
    Fast immer Varianten von $\{a^n b^m c^k \mid \text{lineare (Un-)Gleichung über } n,m,k\}$
\end{bem}

\subsection{Vollständige Klassifikation (reg/nreg und cf/ncf) mit Beweis.}
% \txc{2017H, 2017N, 2019H, 2019N, 2021, 2023P, 2023Z, 2025}

Für jede Sprache beide Fragen beantworten:
nicht regulär $\Rightarrow$ Pumping-Lemma (regulär); kontextfrei $\Rightarrow$ CFG/PDA;
nicht kontextfrei $\Rightarrow$ Pumping-Lemma (kontextfrei).

\begin{itemize}
    \item Ist die folgende Sprache regulär/nicht regulär, kontextfrei/nicht kontextfrei und warum:
    \begin{itemize}
        \item $\{\ol{x} \in \{\un{a}, \un{b}\}^* \mid  \ol{x} = a^n b^n \text{ mit } n \in \nz\} \cup \{ \ol{x} \in \{\un{c}, \un{d}\}^* \mid |\ol{x}| \leq 42 \}$ \txc{(HK2017)}
        \item
        $\{ \ol{z}  \in \{\un{a}, \un{b}, \un{c}\}^* \mid Anzahl(\un{a}, \ol{z}) = Anzahl(\un{b}, \ol{z}) = Anzahl(\un{c}, \ol{z}) \}$ \txc{(NK2017)}
        \item $\{a^m b^n c^k \mid n,m,k \in \nz_0, m=k \}$ \txc{(HK2019)}
        \item $\{a^m b^n c^k \mid n,m,k \in \nz_0, m \geq n \geq k \}$ \txc{(HK2019)}
        \item $\{(ab)^n \mid n \in \nz_0 \}$ \txc{(HK2019)}
        \item $\{a^m b^n \mid m+n  k; m,n,k \in \nz_0 \}$ \txc{(NK2019)}
        \item $\{a^m b^n c^k \mid n,m,k \in \nz_0, m=n \}$ \txc{(NK2019)}
        \item $\{a^m b^n c^k \mid n,m,k \in \nz_0, m \leq n \leq k \}$ \txc{(NK2019)}
        \item
        $\{\ol{x} \in \{\un{a}, \un{b},\un{c} \}^* \mid Anzahl(\un{a},\ol{x})+Anzahl(\un{b},\ol{x}) \text{ ist gerade } \land Anzahl(\un{b},\ol{x}) +Anzahl(\un{c},\ol{x}) \text{ ist ungerade }  \}$ \txc{(PK2021)}
        \item $\{a^m b^n c^k \mid n,m,k \in \nz_0, n = m+k\}$ \txc{(PK2021)}
        \item $\{ \ol{x} \in \{\un{a}, \dots, \un{z}\}^* \mid \exists n: |\ol{x}| = 42^n \}$ \txc{(PK2021)}
    \end{itemize}
    \item Ist die folgende Sprache regulär/nicht regulär, kontextfrei/nicht kontextfrei?
    Beweisen Sie Ihre
    Antwort
    \begin{itemize}
        \item $\{\un{a}^n, \un{b}^m, \un{c}^k \mid n \leq m \leq k\}$ \txc{(PK2021)}
        \item  $\{a^n b^m c^k \mid n,m,k \in \nz_0, n+m = k \}$ \txc{(PK2023)}
        \item   $\{a^n b^m c^k \mid n,m,k \in \nz_0, n \leq m \} \cap \{ a^n b^m c^k \mid n,m,k \in \nz_{0}, m \leq k \}$
        \txc{(PK2023)}
        \item $\{ a^n b^m \mid n,m,k \in \nz_0, n+m = k, k > 5 \}$ \txc{(HK2023)}
        \item $\{ a^m b^n c \mid n,m \in \nz_0, n \leq m\} \cap \{a^n b^m c^k \mid n,m,k \in \nz_0, n\leq m \}$ \txc{(HK2023)}
        \item   $\{a^n b^m c^k \mid n,m,k \in \nz_0, n = k \} \cap \{ a^m b^n c \mid n,m \in \nz_0 \}$ \txc{(PK2025)}
        \item $\{a^{2025} b^{2025} c^k \mid k \in \nz_0, k \leq 2025 \}$ \txc{(PK2025)}
    \end{itemize}
\end{itemize}

\subsection{Nicht-Regularität mit dem Pumping-Lemma zeigen.} % \txc{2017H, 2017N, 2023P, 2023Z, 2025}
Typische Sprachen: $\{a^n b^m c^k\mid n=m\}$, $\{a^{2n}b^{4n}\}$, $\{c^4 a^n b^n\}$.
Gefragt ist explizite Anwendung, das Gegenspieler-Schema muss also vollständig sein.

\begin{itemize}
    \item Zeigen Sie durch explizite Verwendung des Pumpinglemmas, dass die folgen-
    de Sprache nicht regulär ist:
    \begin{itemize}
        \item $\{\un{a}^{2n}, \un{b}^{4n} \mid n \in \nz\}$ \txc{(HK2017)}
        \item $\{ \un{c}^4, \un{a}^n, \un{b}^n \}$ \txc{(NK2017)}
        \item $\{a^n b^m c^k \mid n,m,k \in \nz_0, n = k \}$  \txc{(PK2023)}
        \item $\{a^n b^m c^k \mid n,m,k \in \nz_0, n = m \}$  \txc{(NK2023)}
        \item $\{a^n b^k c^m \mid n,m,k \in \nz_0, n = k\}$ \txc{(PK2025)}
    \end{itemize}
\end{itemize}

\subsection{Kontextfreiheit durch Angabe einer kontextfreien Grammatik zeigen.} % \txc{2017N, 2021, 2023P, 2023Z,2025}
Für $L_1$ aus dem Pumping-Teil wird meist direkt danach die passende CFG verlangt.

\begin{itemize}
    \item Geben Sie eine explizite kontextfreie Grammatik für L1 (Teilaufgabe \ldots)) an.
    \begin{itemize}
        \item $\{a^n b^m c^k \mid n,m,k \in \nz_0, n = k \}$  \txc{(PK2023)}
        \item $\{a^n b^m c^k \mid n,m,k \in \nz_0, n = m \}$  \txc{(NK2023)}
        \item  $\{a^n b^k c^m \mid n,m,k \in \nz_0, n = k\}$ \txc{(PK2025)}
    \end{itemize}
\end{itemize}


\subsection{Regularität durch regulären Ausdruck (oder DEA/rechtslin.\ Grammatik) zeigen.}% \txc{2017H, 2017N}
z.\,B.\ $\{x\in\{a,b\}^*\mid \#_a(x)\text{ gerade}\}$ oder $\#_a+\#_b\ge 2$.
Konstruktives Vorgehen: RegExp konkret hinschreiben.

\begin{itemize}
    \item Zeigen Sie durch die Konstruktion eines regulären Ausdrucks, dass die folgende Sprache regulär ist
    \begin{itemize}
        \item $\{ \ol{x} \in \{\un{a}, \un{b}\}^* \mid Anzahl(\un{a}, \ol{x}) gerade \}$ \txc{(HK2017)}
        \item
        $\{ \ol{x}  \in \{\un{a}, \un{b}, \un{c}\}^* \mid Anzahl(\un{a}, \ol{x}) = Anzahl(\un{b}, \ol{x}) \geq 2 \}$
        \txc{(NK2017)}
    \end{itemize}
\end{itemize}

\subsection{Schnitt/Vereinigung zweier Sprachen aka ``erst den Schnitt berechnen''.} % \txc{2023P, 2023Z, 2025}
\begin{itemize}
    \item $\{\ol{x} \in \{\un{a}, \un{b}\}^* \mid  \ol{x} = a^n b^n \text{ mit } n \in \nz\} \cup \{ \ol{x} \in \{\un{c}, \un{d}\}^* \mid |\ol{x}| \leq 42 \}$ \txc{(HK2017)}
    \item  $\{a^n b^m c^k \mid n,m,k \in \nz_0, n \leq m \} \cap \{ a^n b^m c^k \mid n,m,k \in \nz_{0}, m \leq k \}$
    \txc{(PK2023)}
    \item $\{ a^m b^n c \mid n,m \in \nz_0, n \leq m\} \cap \{a^n b^m c^k \mid n,m,k \in \nz_0, n\leq m \}$ \txc{(HK2023)}
    \item   $\{a^n b^m c^k \mid n,m,k \in \nz_0, n = k \} \cap \{ a^m b^n c \mid n,m \in \nz_0 \}$ \txc{(PK2025)}
\end{itemize}


\subsection{Grammatik für Vereinigung/Konkatenation zweier Sprachen.}
% \txc{2017H (CFG, $\cup$), 2017N (rechtslin., $\cdot$)}
Allgemeine Konstruktion $G_\cup$ bzw.\ $G_\bullet$ aus zwei gegebenen Grammatiken angeben.

\begin{itemize}
    \item Seien $G$ und $G'$ rechtslineare Grammatiken.
    Geben Sie eine rechtslineare Grammetik $G_{\circ}$ an, die
    genau $L(G) \circ L(G')$ erzeugt. \txc{(NK2017)}
    \item Seien $G$ und $G'$ kontextfreie Grammatiken.
    Geben Sie eine kontextfreie Grammetik $G_{\cup}$ an, die
    genau $L(G) \cup L(G')$ erzeugt. \txc{(HK2017)}
\end{itemize}

\subsection{Sprachen mit festen Schranken / endliche Sprachen.}
% \txc{2017H ($|x|\le 42$), 2021 ($|x|=42n$), 2025 ($a^{2025}b^{2025}c^k,\,k\le 2025$)}
Trickfrage: feste Exponenten bzw.\ obere Schranke $\Rightarrow$ die Sprache ist
endlich bzw.\ regulär, hier nicht auf das Pumping-Lemma hereinfallen!

\begin{itemize}
    \item $\{\ol{x} \in \{\un{a}, \un{b}\}^* \mid  \ol{x} = a^n b^n \text{ mit } n \in \nz\} \cup \{ \ol{x} \in \{\un{c}, \un{d}\}^* \mid |\ol{x}| \leq 42 \}$ \txc{(HK2017)}
    \item $\{ \ol{x} \in \{\un{a}, \dots, \un{z}\}^* \mid \exists n: |\ol{x}| = 42^n \}$ \txc{(PK2021)}
    \item $\{a^{2025} b^{2025} c^k \mid k \in \nz_0, k \leq 2025 \}$ \txc{(PK2025)}
\end{itemize}

\begin{warn} $\ $
\begin{itemize}
    \item \textbf{Endlich $\Rightarrow$ regulär.} $a^{2025}b^{2025}c^{k},\,k\le2025$ hat nur endlich viele Wörter
    $\Rightarrow$ regulär (und damit kontextfrei).
    Die festen $2025$ sind Konstanten, keine Variablen!
    \item Das Pumping-Lemma ist \textbf{nur notwendig}, nicht hinreichend.
    Es beweist Nicht-Regularität,
    \textbf{niemals} Regularität.
    \item ``Regulär $\Rightarrow$ nach dem Schnitt bestimmen. $\{n=k\}\cap\{\dots\}$ erzwingt oft
    $n=m=k$, was typischerweise nicht kontextfrei ist.
\end{itemize}
\end{warn}

\section{Endliche Automaten}\label{subsec:endliche-automaten}

\subsection{NEA aus rechtslinearer Grammatik konstruieren (als Diagramm).}%\txc{2019H, 2019N, 2021, 2023P, 2023Z,2025}
Nichtterminale $\to$ Zustände, ein zusätzlicher Finalzustand $f$ für Regeln $S\Coloneqq a$.

\begin{itemize}
    \item Geben Sie zu folgender rechtslinearer Grammatik einen äquivalenten NEA (als Diagramm) an:
    \begin{itemize}
        \item $G=(\{S_0,S_1\},\{a,b,c\},\{S_0\Coloneqq bS_0\mid aS_1\mid bS_1\mid a,\ S_1\Coloneqq aS_0\mid b\},S_0)$ \txc{(PK2021)}
        \item $G_1=(\{S_0,S_1\},\{a,b\},\{S_0\Coloneqq aS_1\mid bS_0\mid bS_1\mid a,\ S_1\Coloneqq aS_0\mid b\},S_0)$ \txc{(PK2023)}
        \item $G\coloneqq(\{S_0,S_1,S_2,S_3\},\{a,b,c\},R,S_0)$ mit\\
        $R\coloneqq\{S_0\Coloneqq cS_0\mid c\mid aS_1\mid bS_1,\ S_1\Coloneqq cS_1\mid aS_2\mid bS_2,\ S_2\Coloneqq cS_2\mid aS_3\mid bS_3\mid a,\ S_3\Coloneqq cS_3\mid c\}$ \txc{(NK2023)}
        \item $G\coloneqq(\{S_0,S_1,S_2\},\{a,b,c\},R,S_0)$ mit\\
        $R\coloneqq\{S_0\Coloneqq aS_0\mid cS_0\mid aS_1,\ S_1\Coloneqq cS_1\mid aS_2\mid bS_2,\ S_2\Coloneqq aS_2\mid bS_2\mid a\mid b\}$ \txc{(PK2025)}
    \end{itemize}
    \item Betrachten Sie die rechtslineare Grammatik G. Vervollständigen Sie gemäß der Konstruktion in der Vorlesung
    folgenden NEA, der genau die Sprache erkennen soll, die die Grammatik erzeugt.
    \begin{itemize}
        \item $G=(\{A_0,A_1,B_0,B_1\},\{a,b\},\{A_0\Coloneqq aA_1\mid B_0,\ A_1\Coloneqq aA_0\mid B_1\mid a,\ B_0\Coloneqq bB_1\mid\epsilon,\ B_1\Coloneqq bB_0\},A_0)$ \txc{(HK2019)}
        \item $G=(\{A_0,A_1,B_0,B_1\},\{a,b\},\{A_0\Coloneqq aA_1\mid B_0,\ A_1\Coloneqq aA_0\mid B_1\mid a,\ B_0\Coloneqq bB_1\mid\epsilon,\ B_1\Coloneqq bB_0\},A_0)$ \txc{(NK2019)}
    \end{itemize}
    \item Geben Sie einen
\end{itemize}

\subsection{Potenzmengenkonstruktion NEA $\to$ DEA.}%\txc{2017H, 2017N, 2019H, 2019N, 2021, 2023P, 2023Z, 2025}
Fast jede Klausur\ldots Mal als Diagramm, mal als Übergangstabelle.
Müllzustand $\emptyset$ mitnehmen.
Zustände = Teilmengen

\begin{itemize}
    \item Geben Sie zu folgendem NEA einen DEA an (Tupel oder Diagramm bzw.\ vollständige Übergangstabelle),
    der die gleiche Sprache akzeptiert.
    \begin{itemize}
        \item \txc{(HK2017)} NEA über $\{a,b\}$, \% Diagramm:\ $\Phi=\{q_0,q_1\}$, Start $q_0$;
        $q_0\xrightarrow{b}q_0,\ q_0\xrightarrow{a,b}q_1,\ q_1\xrightarrow{a}q_1,\ q_1\xrightarrow{b}q_0$; $F=\{q_1\}$
        \item \txc{(NK2017)} NEA über $\{a,b\}$, \% Diagramm:\ $\Phi=\{q_0,q_1\}$, Start $q_0$;
        $q_0\xrightarrow{a}q_0,\ q_0\xrightarrow{b}q_1,\ q_1\xrightarrow{b}q_1,\ q_1\xrightarrow{a,b}q_0$
       % \; (alle Zustände der Konstruktion inkl.\ Müllzustand angeben)
        \item \txc{(PK2021)} NEA über $\{a,b\}$, \% Diagramm:\ $\Phi=\{q_0,q_1,q_2\}$, Start $q_0$;
        $q_0\xrightarrow{a}q_2,\ q_0\xrightarrow{a}q_1,\ q_1\xrightarrow{a}q_0,\ q_1\xrightarrow{a,b}q_2,\ q_2\xrightarrow{b}q_2$
        %\; (nur erreichbare Zustände + Müllzustand nötig)
        \item \txc{(PK2023)} DEA aus dem NEA von Teilaufgabe a) (Grammatik $G_1$)
        \item \txc{(NK2023)} NEA $A$ über $\{a,b\}$, \% Diagramm:\ Start $q_0$;\\
        $\Delta$: $\ \delta(\{q_0\},a)=\{q_1,q_2\},\ \delta(\{q_0\},b)=\{q_0\},\ \delta(\{q_1\},a)=\{q_0\},\ \delta(\{q_1\},b)=\{q_2\},\ \delta(\{q_2\},a)=\emptyset,\ \delta(\{q_2\},b)=\emptyset$;
       % \; Übergangstabelle ergänzen, Start-/Finalzustände angeben
        \item \txc{(PK2025)} DEA aus dem NEA von Teilaufgabe a) (6 Pkt., als Diagramm)
    \end{itemize}
\end{itemize}

\subsection{DEA direkt für eine gegebene Sprache angeben.} %\txc{2017H, 2017N}
z.\,B.\ ``$\#_a(x)$ ungerade'' oder ``$\#_a(x)\equiv 1 \pmod 4$'' aka Zähl-Automat modulo\,$k$.
\begin{itemize}
    \item Geben Sie einen DEA (als Automatendiagramm) an für die Sprache:
    \begin{itemize}
        \item  $\{x\in\{a,b,c\}^*\mid \#_a(x)\text{ ist ungerade}\}$ \txc{(HK2017)}
        \item  $\{x\in\{a,b\}^*\mid \#_a(x)\text{ ist mit Rest }1\text{ durch }4\text{ teilbar}\}$ \txc{(NK2017)}
    \end{itemize}
\end{itemize}

\subsection{Komplement-Automat konstruieren.} % \txc{2019H, 2019N, 2021, 2023P}
Finalzustände tauschen nur beim vollständigen DEA korrekt!

\begin{itemize}
    \item Konstruieren Sie einen NEA $A_C$ (als Diagramm) mit $L(A_C)=L(A)^C$.
    \begin{itemize}
        \item $A$ \% Diagramm über $\{a,b,c\}$, $\Phi=\{q_0,q_1,q_2,q_3\}$, Start/Final $q_0$;
        $q_0\xrightarrow{a,c}q_1,\ q_1\xrightarrow{a}q_0,\ q_1\xrightarrow{b}q_1,\ q_1\xrightarrow{c}q_2,\ q_0\xrightarrow{b}q_2,\ q_2\xrightarrow{c}q_1,\ q_2\xrightarrow{a}q_3,\ q_2\xrightarrow{b}q_2,\ q_3\xrightarrow{a,b,c}q_3$ \txc{(PK2021)}
    \end{itemize}
    \item Gegeben ein endlicher Automat $A=(\Phi,\Sigma,\Delta,q_0,F)$.
    Betrachte $A_C=(\Phi,\Sigma,\Delta,q_0,\Phi\setminus F)$.
    Unter welcher Annahme gilt $L(A_C)=L(A)^C$? Warum ist diese Annahme notwendig? \txc{(PK2023)}
    \item Gegeben ein DEA $A=(\Phi,\Sigma,\delta,q_0,F)$.
    Konstruieren Sie einen DEA (in Tupelnotation),
    der genau das Komplement von $L(A)$ akzeptiert.
    Begründen Sie. \txc{(HK2019)} \txc{(NK2019)}
\end{itemize}


\subsection{Kleene-Stern $L(A)^*$ bzw.\ $L(A)\cdot\{a\}$ konstruieren.} %\txc{2017H, 2017N, 2019N, 2023P}
$\varepsilon$-Kanten von Finalzuständen zurück zum (neuen) Start; neuer akzeptierender Startzustand.

\subsection{Konkatenation $L(A_1)\cdot L(A_2)$ / Vereinigung $L(A_1)\cup L(A_2)$.}% \txc{2019H, 2023Z (Konkat.);2025 (Vereinig.)}
Diagramm und allgemeine Tupel-Konstruktion (disjunkte Zustandsmengen $\Phi_1\cap\Phi_2=\emptyset$).
\begin{itemize}
    \item Wie kann man allgemein zu einem NEA $A$ einen NEA $A'$ konstruieren mit $L(A')=(L(A))^*$?
    Demonstrieren Sie das Vorgehen am NEA aus Teilaufgabe b). \txc{(HK2017)}
    \item Wie kann man allgemein zu einem NEA $A$ einen NEA $A'$ konstruieren mit $L(A')=L(A)\cdot\{a\}$?
    Demonstrieren Sie am NEA aus Teilaufgabe b). \txc{(NK2017)}
    \item Konstruieren Sie einen NEA $A_*$ mit $L(A_*)=L(A)^*$ (in Tupelnotation).
    Erläutern Sie, wie $A_*$
    ein Wort $\overline{w_1}\dots\overline{w_n}$ mit $\overline{w_i}\in L(A)$ ableitet (explizite Rechnung).
    \begin{itemize}
        \item für allgemeines $A=(\Phi,\Sigma,\Delta,q_0,F)$ \txc{(PK2023)}
        \item NEA für $L(A)^*$ (Tupelnotation), Begründung \txc{(NK2019)}
    \end{itemize}
\end{itemize}


\subsection{Äquivalenz Grammatik $\leftrightarrow$ Automat beweisen/widerlegen.}%\txc{2021, 2023P, 2023Z, 2025}
$L(G_1)=L(G_2)$? oder $L(A)\subsetneq L(G)$? Gegenbeispielwort finden oder Inklusion beider Richtungen.

\begin{itemize}
    \item Beweisen oder widerlegen Sie, dass die von $G_2$ erzeugte Sprache mit der von $G_1$ übereinstimmt
    ($L(G_1)=L(G_2)$, beide als Sprachen über $\{a,b,c\}$):
    \begin{itemize}
        \item  $G_2=(\{S_0',S_1',S_2',S_3'\},\{a,b,c\},R,S_0')$,\\
        $R\coloneqq\{S_0'\Coloneqq aS_1'\mid bS_0'\mid cS_2'\mid a,\ S_1'\Coloneqq aS_0'\mid bS_0'\mid cS_2'\mid b,\ S_2'\Coloneqq aS_3',\ S_3'\Coloneqq aS_2'\mid bS_2'\}$ \txc{(PK2023)}
    \end{itemize}
    \item Beweisen oder widerlegen Sie: Die von der Grammatik $G'$ erzeugte Sprache ist die vom Automaten $A$
    (aus Teilaufgabe c) akzeptierte Sprache, d.h.\ $L(A)=L(G')$.
    \begin{itemize}
        \item
        $G'=(\{S_0',S_1',S_2'\},\{a,b\},\{S_0'\Coloneqq aS_1'\mid aS_2'\mid bS_0'\mid a,\ S_1'\Coloneqq aS_0'\mid bS_2'\mid b,\ S_2'\Coloneqq bS_1'\},S_0')$ \txc{(NK2023)}
    \end{itemize}
    \item Beweisen Sie: Die von der Grammatik $G$ erzeugte Sprache steht in echter Inklusion zur vom Automaten
    $A_3$ akzeptierten Sprache, d.h.\ $L(A_3)\subsetneq L(G)$.
    \begin{itemize}
        \item $G=(\{S_0',S_1',S_2',S_3'\},\{a,b,c\},R,S_0')$,\\
        $R\coloneqq\{S_0'\Coloneqq bS_0'\mid aS_1'\mid bS_1'\mid aS_2'\mid S_3',\ S_1'\Coloneqq aS_0'\mid b,\ S_2'\Coloneqq aS_1'\mid b,\ S_3'\Coloneqq S_3'\mid\epsilon\}$.\\
        $A_3$ \% Diagramm über $\{a,b\}$: $q_0,q_1,q_2$ mit Kanten $b,\,a,\,a,b,\,b$. \txc{(PK2025)}
    \end{itemize}
    \item Geben Sie zu dem NEA aus Aufgabenteil c eine rechtslineare Grammatik an, die genau die vom NEA
    akzeptierte Sprache erzeugt, und erzeugen Sie mit dieser Grammatik das Wort $\underline{aabbb}$. \txc{(PK2021)} (NEA $A$ aus 2c, s.\,o.)
\end{itemize}

\subsection{Deterministischen Ersatzakzeptor vervollständigen.}
Schon lange her $\ldots$ vermutlich nicht wieder?
\begin{itemize}
    \item Betrachten Sie den NEA auf der linken Seite.
    Vervollständigen Sie gemäß der Konstruktion aus
    der Vorlesung den deterministischen Ersatzakzeptor (rechts vorgegebene Zustände).
    \begin{itemize}
        \item NEA \% Diagramm:\ $q_0\xrightarrow{a}q_0,\ q_0\xrightarrow{b}q_1,\ q_1\xrightarrow{a}q_1,\ q_1\xrightarrow{\epsilon}q_0$;
        vorgegebene DEA-Zustände $\{q_1\},\{q_0\},\{q_0,q_1\}$  \txc{(HK2019)}
        \item NEA \% Diagramm:\ $q_0\xrightarrow{a}q_0,\ q_0\xrightarrow{b}q_1,\ q_1\xrightarrow{b}q_2,\ q_2\xrightarrow{a}q_2,\ q_2\xrightarrow{\epsilon}q_0$;
        vorgegebene DEA-Zustände $\{q_0\},\{q_1\},\{\,\},\{q_0,q_2\},\{q_2\}$ \txc{(NK2019)}
    \end{itemize}
\end{itemize}

\section{Kontextfreie Grammatiken \& LR/Kellerautomaten}\label{subsec:kontextfreie-grammatiken-&-lr/kellerautomaten}

\subsection{Eindeutigkeit}
Eindeutig $=$ jedes Wort hat genau einen Strukturbaum.

\begin{itemize}
    \item Ist die Grammatik eindeutig?
    Begründen Sie.
    \begin{itemize}
        \item  $G=(\{S\},\{a,b\},\{S\Coloneqq aSSa\mid b\mid a\},S)$ \txc{(HK2017)}
        \item $G=(\{S,B\},\{a,b\},\{S\Coloneqq aBSa\mid\epsilon,\ B\Coloneqq bS\mid Sb\},S)$ \txc{(NK2017)}
        \item  $G=(\{S,B\},\{a,b,c\},\{S\Coloneqq aSc\mid B,\ B\Coloneqq bB\mid\epsilon\},S)$ \txc{(HK2019)}
        \item $G=(\{S,B\},\{a,b,c\},\{S\Coloneqq aSc\mid B,\ B\Coloneqq bB\mid\epsilon\},S)$ \txc{(NK2019)}
        \item  $G=(\{S,B\},\{a,b,c\},\{S\Coloneqq aScB\mid B,\ B\Coloneqq bB\mid b\},S)$ \txc{(PK2021)}
        \item $G=(\{S\},\{a,b,c\},\{S\Coloneqq bSSb\mid c\mid b\},S)$ \txc{(PK2025)}
    \end{itemize}
\end{itemize}

\subsection{Regeln des zur Grammatik gehörenden LR-Automaten explizit angeben.}
Shift-Regeln (Terminal lesen) und Reduce-Regeln (pro Grammatikregel).

\begin{itemize}
    \item Geben Sie die Regeln des zur Grammatik G gehörenden LR-Automaten an
    \begin{itemize}
        \item  $G=(\{S\},\{a,b\},\{S\Coloneqq aSSa\mid b\mid a\},S)$ \txc{(HK2017)}
        \item $G=(\{S,B\},\{a,b\},\{S\Coloneqq aBSa\mid\epsilon,\ B\Coloneqq bS\mid Sb\},S)$ \txc{(NK2017)}
        \item  $G=(\{S,B\},\{a,b,c\},\{S\Coloneqq aSc\mid B,\ B\Coloneqq bB\mid\epsilon\},S)$ \txc{(HK2019)}
        \item $G=(\{S,B\},\{a,b,c\},\{S\Coloneqq aSc\mid B,\ B\Coloneqq bB\mid\epsilon\},S)$ \txc{(NK2019)}
        \item  $G=(\{S,B\},\{a,b,c\},\{S\Coloneqq aScB\mid B,\ B\Coloneqq bB\mid b\},S)$ \txc{(PK2021)}
        \item  $G=(\{S\},\{(,a,)\},\{S\Coloneqq(SS)\mid aS\mid a\},S)$ \txc{(PK2023)}
        \item $G=(\{S\},\{a,b\},\{S\Coloneqq aSSa\mid a\mid b\},S)$ \txc{(NK2023)}
        \item $G=(\{S\},\{a,b,c\},\{S\Coloneqq bSSb\mid c\mid b\},S)$ \txc{(PK2025)}
    \end{itemize}
\end{itemize}

\subsection{Ein Wort mit dem LR-Automaten rechnen und den Strukturbaum angeben.}
Jeden Shift/Reduce-Schritt (Kellerinhalt) angeben.

\begin{itemize}
    \item Rechnen Sie das Wort $w$ und geben Sie ggf.\ den Strukturbaum an:
    \begin{itemize}
        \item  $w=\underline{abba}$ (mit Strukturbaum) \txc{(HK2017)}
        \item  $w=\underline{aba}$ (mit Strukturbaum) \txc{(NK2017)}
        \item  $w=\underline{aabbcbbcb}$ \txc{(PK2021)}
        \item  $w=\underline{(a(aaa))}$ (mit Strukturbaum) \txc{(PK2023)}
        \item $w=\underline{abba}$ (mit Strukturbaum) \txc{(NK2023)}
        \item  $w=\underline{bcbccbb}$ (jeden Schritt der Rechnung angeben) \txc{(PK2025)}
    \end{itemize}
    \item Geben Sie einen Strukturbaum für das Wort in dieser Grammatik an:
    \begin{itemize}
        \item  $w=\underline{aabcc}$ \txc{(HK2019)}
        \item $w=\underline{aabcc}$  \txc{(NK2019)}
    \end{itemize}
\end{itemize}

\subsection{CYK-Algorithmus (Cocke-Kasami-Younger). Grammatik in CNF.}
\begin{itemize}
    \item Prüfen Sie mit dem CYK-Algorithmus, ob $w\in L(G)$ (Grammatik aus Teilaufgabe c bzw.\ gegeben):
    \begin{itemize}
        \item  $w=(a(aaa))$ für $G=(\{S\},\{(,a,)\},\{S\Coloneqq(SS)\mid aS\mid a\},S)$ \txc{(PK2023)}
        \item $G_1=(\{A,B,C\},\{a,b,c\},\{A\Coloneqq AB\mid AC\mid BC\mid a,\ B\Coloneqq BB\mid BC\mid b,\ C\Coloneqq c\},A)$; prüfe $\underline{acb}\in L(G_1)$ \txc{(NK2023)}
        \item $G_{\mathrm{CNF}}=(\{A,B,C\},\{a,b,c\},\{A\Coloneqq AB\mid BC\mid a,\ B\Coloneqq BB\mid b,\ C\Coloneqq AC\mid CC\mid c\},B)$;\\
        (i) zeigen Sie $w=ccabbc\notin L(G_{\mathrm{CNF}})$; \ (ii) für welchen Startzustand würde $w$ akzeptiert? \txc{(PK2025)}
    \end{itemize}
\end{itemize}

\subsection{Kellerautomat vervollständigen und Rechnung angeben.}
Regeln $a\,q_0\,b\Coloneqq\overline{w}\,q_1$ graphisch als $(a,b)/\overline{w}$.
\begin{itemize}
    \item Vervollständigen Sie den (indeterministischen) Kellerautomaten, der dieselbe Sprache erkennt wie $G$,
    und geben Sie die Rechnung für das Wort an.
    \begin{itemize}
        \item Zustände $q_a,q_b,q_c,f$ (Diagramm), Rechnung für $\underline{aabcc}$ \txc{(HK2019)}
        \item  Regelschablonen $(\#,a)/\_,\ (\#,b)/\_,\ (a,a)/\_,\ (a,b)/\_,\ (a,c)/\_$ und $\#/\epsilon$ nach $f$;
        Rechnung für $\underline{aabcc}$ \txc{(NK2019)}
    \end{itemize}
\end{itemize}

\subsection{Induktionsbeweis über Wörter / Sprachcharakterisierung.}
Induktion über den Ableitungsbaum bzw.\ die Regelanwendungen.
\begin{itemize}
    \item Zeigen Sie durch Induktion, dass für alle $\overline{x}\in L(G)$ gilt:
    \begin{itemize}
        \item  $\#_a(\overline{x})\ge\#_b(\overline{x})$ \; oder \; $\overline{x}=b$ \txc{(HK2017)}
        \item $\#_a(\overline{x})\ge\#_b(\overline{x})$ \txc{(NK2017)}
        \item $\#_b(\overline{w})\le\#_a(\overline{w})+1$ \txc{(NK2023)}
    \end{itemize}
    \item Zeigen Sie, dass für jedes Wort die Anzahl der $b$'s mindestens um $1$ größer ist als die Anzahl der $a$'s. \txc{(PK2021)}
    \item Betrachten Sie die Sprache $L'=\{\overline{w}\in\{(,a,)\}^*\mid \#_((\overline{x})\ge\#_)(\overline{x})$ und
    $\#_((\overline{y})\le\#_)(\overline{y})$ für alle Zerlegungen $\overline{w}=\overline{x}\overline{y}\}$.
    Zeigen Sie $L(G)\subsetneq L'$. \txc{(PK2023)}
\end{itemize}

\section{Probleme / Berechenbarkeit \& Reduktion}\label{subsec:probleme-/-berechenbarkeit-&-reduktion}

\subsubsection{P, NP, NP-vollständig umgangssprachlich erklären.}
\begin{itemize}
    \item Erläutern Sie umgangssprachlich in je einem Satz die Konzepte P, NP und NP-vollständig. \txc{(PK2021)} \quad \txc{(PK2023)}
\end{itemize}

\subsubsection{Implikationen aus $A\le_p B$ beurteilen (unter $P\ne NP$).}
Bei ``falsch'' Gegenbeispiel, bei ``wahr'' Beweis.
\begin{itemize}
    \item Geben Sie an, ob die Implikationen korrekt sind (formal begründen):
    \begin{itemize}
        \item $A\in\mathrm{P}\Rightarrow B\in\mathrm{P}$; \; $A\in\mathrm{NP}\Rightarrow B\in\mathrm{NP}$; \; $A$
        NP-vollständig $\Rightarrow B$ NP-vollständig \txc{(HK2017)}
        \item $B\in\text{Chomsky-2}\Rightarrow A\in\mathrm{P}$; \; $A\in\mathrm{NP}\Rightarrow B$ NP-vollständig; \; $A$ NP-vollständig $\Rightarrow B$ NP-hart  \txc{(NK2017)}
        \item  $B\in\mathrm{NP}\Rightarrow A\in\mathrm{P}$; \; $B\in\mathrm{P}\Rightarrow A\in\mathrm{NP}$; \; $B$
        NP-hart $\Rightarrow A$ NP-vollständig \txc{(HK2019)}
        \item $B\in\mathrm{NP}\Rightarrow A\in\mathrm{P}$; \; $B\in\mathrm{P}\Rightarrow A\in\mathrm{NP}$; \; $B$
        NP-hart $\Rightarrow A$ NP-vollständig \txc{(NK2019)}
        \item (i) $B\in\mathrm{NP}\Rightarrow A\in\mathrm{P}$; \ (ii) $A$ NP-vollständig $\Rightarrow B$ NP-hart  \txc{(NK2023)}
        \item (i) $B\in\mathrm{NP}\Rightarrow A\in\mathrm{P}$; \ (ii) $A\in\mathrm{P}\Rightarrow B$ NP-vollständig \txc{(PK2025)}
    \end{itemize}
\end{itemize}

\subsubsection{Sprachklassen der Größe nach ordnen / Teilmengenbeziehungen.}
$\subsetneq$ oder $=$; ggf.\ Beispielsprachen.
\begin{itemize}
    \item Ordnen Sie die folgenden Mengen/Probleme der Größe nach ($\subsetneq$ oder $=$):
    \begin{itemize}
        \item (i) entscheidbar, (ii) endlicher Automat, (iii) rekursiv aufzählbar, (iv) Chomsky-0, (v) kontextfrei \txc{(NK2023)}
        \item  (i) LR($k$), $k>0$, (ii) DEA, (iii) indeterministische Turingmaschine, (iv) Chomsky-2, (v)
        Turingmaschine \txc{(PK2025)}
    \end{itemize}
    \item Welche Teilmengenbeziehungen gelten für P, PSPACE und Chomsky-2? Welche Mengen sind echt größer? Warum? \txc{(PK2021)} \quad \txc{(PK2023)}
    \item Geben Sie je ein Beispiel an, das zeigt, dass Chomsky-$i$ eine echte Teilmenge von Chomsky-$(i-1)$
    ist für $i\in\{1,2,3\}$. Geben Sie zudem eine Sprache an, die nicht in Chomsky-0 liegt. \txc{(HK2017)}
    \item Geben Sie für die Teilmengenbeziehungen an, ob korrekt (Beispiel/Gegenbeispiel):
    \begin{itemize}
        \item reguläre Sprachen $\subsetneq$ formale Regelsprachen; \ Kellerautomaten-Sprachen $\subsetneq$ Chomsky-3;
        \ (det.\ TM entscheidbare Sprachen) $\subsetneq$ (det.\ TM akzeptierbare Sprachen) \txc{(NK2017)}
    \end{itemize}
    \item Beschriften Sie alle Mengen im Diagramm mit je einer Sprachklasse und die äußeren Mengen mit
    Beispielsprachen (nicht in der nächstkleineren Menge), die innerste mit einer enthaltenen Beispielsprache.
    \begin{itemize}
        \item Klassen: DPDA, rekursiv aufzählbar, Chomsky-2, entscheidbar \txc{(HK2019)}
        \item  Klassen: DPDA, rekursiv aufzählbar, Chomsky-3, entscheidbar \txc{(NK2019)}
    \end{itemize}
    \item Ordnen Sie die Problemklassen rekursiv aufzählbar, Chomsky-2, Chomsky-0, kontextsensitiv, Chomsky-3. \txc{(HK2020)}
    \item Begründen Sie richtig/falsch: (i) $\mathrm{NP}\subset\mathrm{PSPACE}$; \txc{(PK2025)}
    (ii) die durch deterministische Turingmaschinen akzeptierten Sprachen sind genau die entscheidbaren Sprachen. \txc{(PK2025)}
\end{itemize}

\subsubsection{Prüfen, welche gegebenen Abbildungen $f$ eine Reduktion $A\le_p B$ sind.}
Total? polynomiell (Ausgabelänge!)? $x\in A\Leftrightarrow f(x)\in B$? Typ passend?
\begin{itemize}
    \item Beweisen bzw.\ prüfen Sie, ob die folgenden Abbildungen eine Reduktion $A\le_p B$ sind:
    \begin{itemize}
        \item \txc{(HK2017)} $A=\{(a,b,c,d)\in\N^4\mid a+b=c+d\}$, $B=\{(x,y)\in\N^2\mid x=y\}$;\\
        1) $f:(a,b,c,d)\mapsto(a+b,c+d)$; \ 2) $g:(a,b,c,d)\mapsto(a+c,b+d)$
        \item \txc{(NK2017)} $A=\{(x,y)\in\nz^2\mid x^2=y\}$, $B=\{(a,b,c)\in\nz^3\mid a\cdot b=c\}$;\\
        1) $f(x,y):=(1,x,\tfrac{y}{x})$; \ 2) $g(x,y):=(x,x,y)$
        \item \txc{(PK2023)} $A=\{(a,b,c,d)\in\nz^4\mid a+b=c+d\}$, $B=\{(x,y)\in\nz^2\mid x=y\}$;\\
        (i) $f:(a,b,c,d)\mapsto(a+c,b+d)$; \ (ii) $g:(a,b,c,d)\mapsto(2^{a+b},2^{c+d})$; \ (iii) $h:(a,b,c,d)\mapsto(1,1)$
        \item \txc{(NK2023)} drei Teile:\\
        (i) $A=\{(x,y)\in\nz^2\mid x^2=y\}$, $B=\{(a,b,c)\in\nz^3\mid a^2+(b-1)^2=2(c+1)\}$,
        $f(x,y)=\begin{cases}(2,1,1)&(x,y)\in A\\(0,0,0)&\text{sonst}\end{cases}$\\
        (ii) $A=\{(x,y,z)\in\nz^3\mid \exp(x)2^y=\exp(\log(2)z)\}$, $B=\{(a,b,c)\in\nz^3\mid c\cdot b=a\}$,
        $f(x,y,z)=(\exp(\log(2)z),2^y,\exp(x))$\\
        (iii) $A=\{(x,y)\in\Q^2\mid x^2+y^2=2yx\}$, $B=\{(a,b,c)\in\nz^3\mid a\cdot c=(1+a)b\}$, $f(x,y)=(1,x,2y)$
        \item \txc{(HK2020)} $A=\{(a,b)\in\N^2\mid 2a=b\}$, $B=\{(c,d,e)\in\N^3\mid c=d\cdot e\}$;\\
        1) $f(a,b):=(b,a,2)$; \ 2) $f(a,b):=(a,b,2)$
    \end{itemize}
\end{itemize}

\subsubsection{Eine polynomielle Reduktion selbst angeben und beweisen.}
\begin{itemize}
    \item Geben Sie eine Reduktion $A\le_p B$ an und beweisen Sie:
    \begin{itemize}
        \item  $A=\{(x,y)\in\nz^2\mid x^2=y\}$, $B=\{(a,b,c)\in\nz^3\mid (a-1)b=c-1\}$ \txc{(PK2023)}
        \item  $A=\{(x,y,z)\in\nz^3\mid 2^x\cdot 2^y=4^z\}$, $B=\{(a,b)\in\nz^2\mid 2a=b\}$ \txc{(NK2023)}
    \end{itemize}
    \item Geben Sie explizit \emph{zwei unterschiedliche} polynomielle Reduktionen $f_1,f_2$ an und beweisen Sie:
    \begin{itemize}
        \item  $A=\{(x,y)\in\nz^2\mid x^2=y\}$, $B=\{(a,b,c)\in\nz^3\mid (a-1)b=c-1\}$ \txc{(PK2025)}
    \end{itemize}
    \item Beweisen Sie, dass $f$ eine polynomielle Reduktion ist:
    \begin{itemize}
        \item  $A=\{(a,b)\in\N^2\mid a=b\}$, $B=\{(x,y,z)\in\N^3\mid x^2=y^2=z\}$, $f:(a,b)\mapsto(a,b,a^2)$ \txc{(PK2021)}
    \end{itemize}
    \item Zeigen Sie $\mathrm{IND}\le_p\mathrm{TEAM}$:
    \begin{itemize}
        \item \textbf{TEAM:} Menge $S$ von $n$ Spielern, Menge $T=\{T_1,\dots,T_m\}$ mit $T_i\subset S$, \txc{(PK2021)}
        Zahl $k\le m$. Existieren $k$ Teams, in denen kein Spieler doppelt vorkommt?\\
        \textbf{IND:} ungerichteter Graph $(V,E)$, Zahl $k\le|V|$. Existiert $V'\subseteq V$ mit $|V'|=k$,
        so dass für $u,v\in V'$ gilt $\{u,v\}\notin E$?
    \end{itemize}
    \item Vervollständigen Sie den Beweis $\mathrm{KP}_*\le_p\mathrm{BPP}$:
    \begin{itemize}
        \item
        $\mathrm{BPP}:=\{(\vec c,\vec w)\in\N^m\times\N^n\mid \exists I_1,\dots,I_m\subseteq\{1,\dots,n\}:\ j\ne k\Rightarrow I_j\cap I_k=\emptyset,\ \bigcup_{i=1}^m I_i=\{1,\dots,n\},\ \forall i:\ \sum_{j\in I_i}w_j\le c_i\}$;\\
        $\mathrm{KP}_*:=\{(\vec w,\hat w)\in\N^n\times\N\mid \exists I\subseteq\{1,\dots,n\}:\ \sum_{i\in I}w_i=\hat w\}$.\\
        Ansatz: $f(\vec w,\hat w)=(\vec c,\vec w)$ mit passendem $\vec c$; Äquivalenzkette $\Leftrightarrow$
        ausfüllen. \\ \txc{(HK2019)} \quad \txc{(NK2019)}
    \end{itemize}
\end{itemize}

\section{Multiple-Choice}\label{subsec:multiple-choice}

\subsection{Sprachen \& Automaten}
\begin{mytable}
    \begin{tabular}{@{}p{0.60\textwidth} c p{0.30\textwidth}@{}}
        \hline
        \textbf{Aussage} & \textbf{} & \textbf{Begründung}\\
        \hline
        Jede endliche Sprache ist regulär.
        & W & endlich $\Rightarrow$ RegExp als Aufzählung.\\
        $\exists$ Paar kontextfreier $L_1,L_2$ mit $L_1\cup L_2$ kontextfrei.
        & W & CFL sogar immer unter $\cup$
        abgeschl.\\
        $\exists$ Paar kontextfreier $L_1,L_2$ mit $L_1\cap L_2$ kontextfrei.
        & W & ``es gibt ein Paar'' (z.\,B.\
        regulär); nicht für alle.\\
        Kontextfreie Sprachen sind gegen Schnitt abgeschlossen.
        & F & Gegenbsp.\ $a^n b^n c^n$.\\
        $\exists$ kontextsensitive $L$ mit $L^C$ nicht kontextsensitiv.
        & F & kontextsensitiv unter Kompl.\ abgeschl.\\
        Reg.\ Sprache unendlich $\iff \exists$ Wort $n_0\le|w|<2n_0$ ($n_0=\#$Zust.). & W & Standard-Charakterisierung.\\
        Reg.\ Sprache unendlich, sobald ein Wort der Länge $\ge n_0$ existiert.
        & W & Zustand wiederholt sich $\Rightarrow$ pumpbar.\\
        Reg.\ Grammatik mit $n$ Nichtterm.\ $\Rightarrow$ jeder DEA $\ge n$ Zustände.
        & F & minimaler DEA kann kleiner sein.\\
        LR-/DPDA-Sprachen sind gegen Komplement abgeschlossen.
        & W & DCFL unter Komplement abgeschl.\\
        DPDAs/PDAs gegen Schnitt abgeschlossen.
        & F & schon CFL nicht unter $\cap$.\\
        DPDAs gegen Schnitt mit DEA abgeschlossen.
        & W & DCFL $\cap$ regulär $=$ DCFL.\\
        $\{a^n b^m c^n d\mid m,n\}$ durch DPDA erkennbar.
        & W & deterministisch: $a$ push, $c$ pop, $d$ prüfen.\\
        Für kontextfreie Sprachen ist das Wortproblem nicht entscheidbar.
        & F & CYK entscheidet es ($\mathcal O(n^3)$).\\
        $G=(\{S\},\{1,2\},\{S\Coloneqq S1S\mid 2\},S)$ ist eindeutig.
        & F & z.\,B.\ ``$21212$'' hat zwei Bäume.\\
        Jede links-/rechtslineare Grammatik ist eindeutig.
        & F & reguläre Grammatiken können mehrdeutig sein.\\
        $G:\ S\Coloneqq SS\mid(S)\mid\varepsilon$ ist nicht eindeutig.
        & W & $\varepsilon$/$SS$ erzeugen mehrere Bäume.\\
        \hline
    \end{tabular}
\end{mytable}

\subsection{Berechenbarkeit \& Komplexität}
\begin{mytable}
    \begin{tabular}{@{}p{0.60\textwidth} c p{0.30\textwidth}@{}}
        \hline
        \textbf{Aussage} & \textbf{} & \textbf{Begründung}\\
        \hline
        Probleme in NP sind entscheidbar.
        & W & $\text{NP}\subseteq$ entscheidbar.\\
        Ein unentscheidbares Problem kann nicht in NP sein.
        & W & Kontraposition von oben.\\
        Ein unentscheidbares Problem kann nicht NP-hart sein.
        & F & z.\,B.\ Halteproblem ist NP-hart.\\
        Rekursiv aufzählbare Sprachen werden von det.\ TM akzeptiert.
        & W & det.\ TM $=$ nichtdet.\ TM (r.\,a.).\\
        $\{(a,b,c)\mid \varphi_{a+b}(c)\!\downarrow\}$ ist formale Regelsprache (r.\,a.). & W & Halteproblem ist semi-entscheidbar.\\
        $\{(a,b,c)\mid \varphi_{a+b}(c)\!\downarrow\}$ ist entscheidbar.
        & F & Halteproblem unentscheidbar.\\
        $\{(a,b)\mid \varphi_a(b)\!\uparrow\}$ ist rekursiv aufzählbar.
        & F & Komplement des Halteproblems, nicht r.\,a.\\
        Es gibt abzählbar viele verschiedene Turingmaschinen.
        & W & jede TM endlich kodierbar.\\
        Es gibt überabzählbar viele verschiedene Turingmaschinen.
        & F & siehe oben.\\
        $\text{P}\subseteq\text{PSPACE}$.
        & W & Standardinklusion.\\
        $\text{P}\subset\text{NP}\cap\text{co-NP}$.
        & W\,(?) & $\subseteq$ gilt; echte Inklusion offen.\\
        Menge der entscheidbaren Probleme gegen Schnitt abgeschlossen.
        & W & zwei Entscheider ``und''-verknüpfen.\\
        Für beliebige entscheidbare $L_1,L_2$ ist ``$L_1\subseteq L_2$'' entscheidbar.
        & F & reduziert von Leerheit/Halteproblem.\\
        Für beliebige kontextfreie $L_1,L_2$ ist ``$L_1\subseteq L_2$'' entscheidbar.
        & F & CFL-Inklusion unentscheidbar.\\
        Entscheidbar, ob eine TM nach $\le t$ Schritten hält.
        & W & einfach $t$ Schritte simulieren.\\
        Wortproblem für kontextsensitive Sprachen ist entscheidbar.
        & W & LBA / Chomsky-1.\\
        Formale Regelsprachen abgeschl.\ gegen $\cap,{}^C,\cup,*$.
        & F & Komplement fehlt (r.\,a.\ nicht unter $^C$).\\
        \hline
    \end{tabular}
\end{mytable}

\subsection{Reduktion, NP-Vollständigkeit, Gödelisierung}
\begin{mytable}
    \begin{tabular}{@{}p{0.60\textwidth} c p{0.30\textwidth}@{}}
        \hline
        \textbf{Aussage} & \textbf{} & \textbf{Begründung}\\
        \hline
        $A$ NP-vollständig $\iff A\le_p\text{SAT}$ und $\text{SAT}\le_p A$.
        & W & $A\le_p$SAT$\Rightarrow A\in$NP; SAT$\le_p A\Rightarrow$ NP-hart.\\
        $A$ NP-vollständig $\iff \text{SAT}\le_p A$.
        & F & $A\in$NP fehlt (das wäre nur NP-hart).\\
        $A$ NP-hart $\iff A\le_p\text{SAT}$.
        & F & Richtung falsch (müsste SAT$\le_p A$).\\
        $A$ NP-hart $\iff A\le_p\text{ILP}\wedge\text{ILP}\le_p A$.
        & F & beschreibt NP-vollständig, nicht NP-hart.\\
        Für jede reguläre Sprache $A$ gilt $A\le_p$ 3-SAT. & W & regulär $\subseteq$ P $\subseteq$ NP; 3-SAT NP-vollst.\\
        $A\in\text{Chomsky-2}\Rightarrow$ 2-SAT $\le_p A$.
        & F & Gegenbsp.\ $A=\emptyset$ (kein Ziel für Ja-Instanzen).\\
        Longest-Path (Pfad Länge $\ge k$, knotendisjunkt) ist NP-hart.
        & W & Hamiltonpfad als Spezialfall.\\
        Erreichbarkeit ($\exists$ Pfad $u$-$v$) ist NP-hart.
        & F & in P (BFS/DFS).\\
        Fair teilen (Werte auf $k$ Personen gleich) ist NP-vollständig.
        & W & Partition/Bin-Packing.\\
        Bier auf Gläser ohne Überlauf verteilen.
        & W & Bin-Packing $\Rightarrow$ NP-hart.\\
        Wunsch-Zimmernachbar (je 1 Wunsch) erfüllbar?
        & F & Matching $\Rightarrow$ in P.\\
        $\{1,\dots,5\}^*\to\nz,\ (n_1,\dots,n_t)\mapsto\sum n_i 10^i$ ist Gödelisierung.
        & W\,(?) & Ziffern $<10$ $\Rightarrow$ injektiv; def.-abhängig (nicht surjektiv).\\
        $\nz^*\to\nz,\ (n_1,\dots,n_t)\mapsto\sum n_i 10^i$ ist Gödelisierung.
        & F & unbeschr.\ $n_i$ $\Rightarrow$
        Überträge, nicht injektiv.\\
        Eine Gödelisierung muss polynomiell berechenbar sein.
        & F & nur berechenbar \& injektiv nötig.\\
        Universelle TM mit 42 Zuständen und 42 Bandzeichen.
        & W & UTMs existieren, $42\times42$ reicht üppig.\\
        TM mit 42 Zuständen simulierbar durch TM mit wenigen Zuständen.
        & W\,(?) & Zustands-/Zeichen-Tradeoff;
        Skript prüfen.\\
        Universelle TM mit einem Zustand.
        & (?) & umstritten $\ldots$ im Skript / Tutorium klären.\\
        \hline
    \end{tabular}
\end{mytable}